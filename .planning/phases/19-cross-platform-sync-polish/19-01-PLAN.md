---
phase: 19-cross-platform-sync-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/capabilities/default.json
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src/stores/timer-store.ts
  - apps/desktop/src/stores/goals-store.ts
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - GOAL-06
  - DASH-06

must_haves:
  truths:
    - "Dashboard XP and streak update after timer session completes without page refresh"
    - "Dashboard goals update after creating, completing, or updating progress on a goal"
    - "Tauri plugins for updater, autostart, and process are registered and permitted"
  artifacts:
    - path: "apps/desktop/src-tauri/Cargo.toml"
      provides: "Rust dependencies for updater, autostart, process plugins"
      contains: "tauri-plugin-updater"
    - path: "apps/desktop/src-tauri/src/lib.rs"
      provides: "Plugin registration for updater, autostart, process"
      contains: "tauri_plugin_updater"
    - path: "apps/desktop/src-tauri/capabilities/default.json"
      provides: "Capability permissions for updater, autostart, process"
      contains: "updater:default"
    - path: "apps/desktop/src/stores/timer-store.ts"
      provides: "Dashboard refresh after timer stop/complete API calls"
      contains: "fetchDashboard"
    - path: "apps/desktop/src/stores/goals-store.ts"
      provides: "Dashboard refresh after goal mutations"
      contains: "fetchDashboard"
  key_links:
    - from: "apps/desktop/src/stores/timer-store.ts"
      to: "apps/desktop/src/stores/dashboard-store.ts"
      via: "useDashboardStore.getState().fetchDashboard()"
      pattern: "useDashboardStore\\.getState\\(\\)\\.fetchDashboard"
    - from: "apps/desktop/src/stores/goals-store.ts"
      to: "apps/desktop/src/stores/dashboard-store.ts"
      via: "useDashboardStore.getState().fetchDashboard()"
      pattern: "useDashboardStore\\.getState\\(\\)\\.fetchDashboard"
---

<objective>
Install Tauri plugins (updater, autostart, process) and wire dashboard auto-refresh into timer and goals stores.

Purpose: Lays the plugin infrastructure for Settings page (Plan 02) and ensures dashboard stays current when timer or goals change (DASH-06). Also formally covers SYNC-01/02/03/04 and GOAL-06 which are already satisfied by the shared API/DB architecture.

Output: Rust plugins registered, capabilities granted, dashboard refreshes automatically after timer and goal state changes.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cross-platform-sync-polish/19-RESEARCH.md
@apps/desktop/src-tauri/Cargo.toml
@apps/desktop/src-tauri/src/lib.rs
@apps/desktop/src-tauri/capabilities/default.json
@apps/desktop/src-tauri/tauri.conf.json
@apps/desktop/src/stores/timer-store.ts
@apps/desktop/src/stores/goals-store.ts
@apps/desktop/src/stores/dashboard-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and register Tauri plugins (updater, autostart, process)</name>
  <files>
    apps/desktop/src-tauri/Cargo.toml
    apps/desktop/src-tauri/src/lib.rs
    apps/desktop/src-tauri/capabilities/default.json
    apps/desktop/src-tauri/tauri.conf.json
  </files>
  <action>
    1. Add Rust dependencies to Cargo.toml (under [dependencies]):
       - tauri-plugin-updater = "2"
       - tauri-plugin-autostart = "2"
       - tauri-plugin-process = "2"

    2. Install JS bindings from apps/desktop/:
       pnpm add @tauri-apps/plugin-updater @tauri-apps/plugin-autostart @tauri-apps/plugin-process

    3. Register plugins in lib.rs Builder chain (after existing .plugin(tauri_plugin_notification::init())):
       - .plugin(tauri_plugin_updater::Builder::new().build())
       - .plugin(tauri_plugin_autostart::init(tauri_plugin_autostart::MacosLauncher::LaunchAgent, None))
       - .plugin(tauri_plugin_process::init())
       Add `use tauri_plugin_autostart::MacosLauncher;` if needed for cleaner code.

    4. Add capability permissions to default.json permissions array:
       - "updater:default"
       - "autostart:allow-enable"
       - "autostart:allow-disable"
       - "autostart:allow-is-enabled"
       - "process:allow-restart"

    5. Add updater config to tauri.conf.json:
       - Under "plugins" key (create if not exists): { "updater": { "pubkey": "REPLACE_WITH_GENERATED_PUBKEY", "endpoints": ["https://github.com/OWNER/REPO/releases/latest/download/latest.json"] } }
       - Under "bundle": add "createUpdaterArtifacts": true

    Note: pubkey and endpoint URL are placeholders. Actual key generation and repo URL are pre-release steps, not phase 19 scope.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler/apps/desktop/src-tauri" && cargo check 2>&1 | tail -5</automated>
    <manual>Verify Cargo.toml has 3 new plugin deps, lib.rs has 3 new .plugin() calls, default.json has 5 new permissions</manual>
  </verify>
  <done>All three Tauri plugins compile, are registered in the builder, and have capability permissions granted. Updater config placeholder exists in tauri.conf.json.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard auto-refresh into timer and goals stores</name>
  <files>
    apps/desktop/src/stores/timer-store.ts
    apps/desktop/src/stores/goals-store.ts
  </files>
  <action>
    Wire `useDashboardStore.getState().fetchDashboard()` calls into both stores:

    **timer-store.ts:**
    1. Add import at top: `import { useDashboardStore } from './dashboard-store';`
    2. In stop() -- after the fire-and-forget `apiFetch` PATCH .then() resolves (line ~408-418), add `.then(() => { useDashboardStore.getState().fetchDashboard(); })` chained after the existing .catch. Specifically, change the pattern from `.catch(() => {})` to `.then(() => { useDashboardStore.getState().fetchDashboard(); }).catch(() => {})`.
    3. In completePhase() -- there are THREE places where `apiFetch<StopResponse>` is called with action:'stop':
       a. Flowmodoro break completion (~line 453): in the .then() callback, after `onSessionComplete(...)`, add `useDashboardStore.getState().fetchDashboard();`
       b. Pomodoro target sessions reached (~line 506): in the .then() callback, after `onSessionComplete(...)`, add `useDashboardStore.getState().fetchDashboard();`
       c. These are the only stop-with-XP paths. The non-session-complete transitions (work_done, break_done) don't need dashboard refresh since no XP/streak changes.
    4. In skipFlowBreak() -- after the fire-and-forget apiFetch PATCH (~line 661-672), chain `.then(() => { useDashboardStore.getState().fetchDashboard(); })` before the `.catch(() => {})`.

    **goals-store.ts:**
    1. Add import at top: `import { useDashboardStore } from './dashboard-store';`
    2. In createGoal() -- after `await get().fetchGoals()` (line ~108), add: `useDashboardStore.getState().fetchDashboard();`
    3. In updateProgress() -- after `await get().fetchGoals()` (line ~121), add: `useDashboardStore.getState().fetchDashboard();`
    4. In completeGoal() -- after `await get().fetchGoals()` (line ~131), add: `useDashboardStore.getState().fetchDashboard();`

    Key: Dashboard refresh must fire AFTER the API call resolves (not before), to avoid showing stale data. In timer-store the API calls are fire-and-forget (.then/.catch), so chain fetchDashboard in the .then(). In goals-store the API calls are awaited, so add fetchDashboard() on the line after fetchGoals().
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler/apps/desktop" && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Grep for fetchDashboard in timer-store.ts and goals-store.ts to confirm wiring</manual>
  </verify>
  <done>Dashboard auto-refreshes when: timer session stops or completes (XP/streak update), goal is created, progress updated, or completed. No polling -- purely event-driven.</done>
</task>

</tasks>

<verification>
- `cargo check` passes in src-tauri (plugins compile)
- `npx tsc --noEmit` passes in apps/desktop (TS types valid)
- grep confirms `fetchDashboard` appears in both timer-store.ts and goals-store.ts
- default.json contains updater, autostart, and process permissions
</verification>

<success_criteria>
- Three Tauri plugins installed, registered, and permitted
- Dashboard refreshes after timer stop/complete and goal create/update/complete
- No regressions in existing timer or goals functionality
- SYNC-01/02/03/04 and GOAL-06 covered (already satisfied by shared API/DB)
</success_criteria>

<output>
After completion, create `.planning/phases/19-cross-platform-sync-polish/19-01-SUMMARY.md`
</output>
