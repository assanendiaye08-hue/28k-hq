---
phase: 07-ai-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/modules/ai-assistant/chat.ts
  - src/modules/ai-assistant/memory.ts
  - src/modules/ai-assistant/nudge.ts
  - src/modules/scheduler/briefs.ts
  - src/modules/scheduler/planning.ts
  - src/modules/checkin/ai-categories.ts
  - src/modules/auto-feed/filter.ts
  - src/modules/profile/ai-tags.ts
  - src/modules/resources/tagger.ts
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "No file in the codebase imports OpenRouter directly except src/shared/ai-client.ts"
    - "Every AI call passes a memberId and feature tag for tracking"
    - "Each call site handles the degraded flag by using its existing fallback"
    - "No hardcoded model strings remain in any module file"
  artifacts:
    - path: "src/modules/ai-assistant/chat.ts"
      provides: "Chat handler using callAI"
      contains: "callAI"
    - path: "src/modules/ai-assistant/nudge.ts"
      provides: "Nudge system using callAI"
      contains: "callAI"
    - path: "src/modules/ai-assistant/memory.ts"
      provides: "Conversation memory with callAI for compression"
      contains: "callAI"
    - path: "src/modules/scheduler/briefs.ts"
      provides: "Morning briefs using callAI"
      contains: "callAI"
    - path: "src/modules/scheduler/planning.ts"
      provides: "Sunday planning using callAI"
      contains: "callAI"
    - path: "src/modules/checkin/ai-categories.ts"
      provides: "Category extraction using callAI"
      contains: "callAI"
    - path: "src/modules/auto-feed/filter.ts"
      provides: "Content filter using callAI with memberId='system'"
      contains: "callAI"
    - path: "src/modules/profile/ai-tags.ts"
      provides: "Profile tag extraction using callAI"
      contains: "callAI"
    - path: "src/modules/resources/tagger.ts"
      provides: "Resource tagging using callAI"
      contains: "callAI"
  key_links:
    - from: "all 9 module files"
      to: "src/shared/ai-client.ts"
      via: "import { callAI } from '../../shared/ai-client.js'"
      pattern: "import.*callAI.*from.*shared/ai-client"
---

<objective>
Migrate all 8 existing AI call sites (across 9 files) to use the centralized AI client from Plan 01. Remove all independent OpenRouter client instances and hardcoded model strings.

Purpose: This completes INFRA-01 -- after this plan, every AI call in the codebase routes through the centralized client with automatic token tracking, budget enforcement, and model routing. No direct OpenRouter imports remain outside `src/shared/ai-client.ts`.

Output: 9 modified module files, all using `callAI()` from `src/shared/ai-client.ts`.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-infrastructure/07-CONTEXT.md
@.planning/phases/07-ai-infrastructure/07-RESEARCH.md
@.planning/phases/07-ai-infrastructure/07-01-SUMMARY.md
@src/shared/ai-client.ts
@src/shared/ai-types.ts
@src/shared/ai-templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate chat, memory, and nudge (ai-assistant module)</name>
  <files>src/modules/ai-assistant/chat.ts, src/modules/ai-assistant/memory.ts, src/modules/ai-assistant/nudge.ts</files>
  <action>
**For each of these 3 files, apply the same migration pattern:**

1. **Remove:** The `import { OpenRouter } from '@openrouter/sdk'` import
2. **Remove:** The `let openrouterClient` variable and `getOpenRouterClient()` function (entire block)
3. **Remove:** Any hardcoded model string constants (`PRIMARY_MODEL`, `FALLBACK_MODEL`)
4. **Add:** `import { callAI } from '../../shared/ai-client.js'`
5. **Add:** `import type { ExtendedPrismaClient } from '../../db/client.js'` if not already present
6. **Replace** each `client.chat.send(...)` call with `callAI(db, { memberId, feature, messages, responseFormat? })`
7. **Handle** `result.degraded === true` by using each file's existing fallback

**File-specific details:**

**chat.ts:**
- Feature tag: `'chat'`
- memberId: already available as function parameter
- db: already available as function parameter
- Remove `PRIMARY_MODEL` and `FALLBACK_MODEL` constants (lines 36-39)
- Remove `import { config } from '../../core/config.js'` if only used for OPENROUTER_API_KEY (check if `config` is used for anything else first -- it's used for nothing else in chat.ts, but verify)
- The existing fallback text `"My circuits are a bit fried right now..."` is the degradation response -- use it when `result.degraded` is true
- The `DAILY_MESSAGE_CAP` of 50 messages/day stays as-is (separate from token budget -- it's a message count limit, not a token limit)

**memory.ts:**
- Feature tag: `'summary'` (for the compression call in `compressSummary`)
- memberId: already available as function parameter
- db: already available as function parameter
- The only AI call is in `compressSummary()` -- replace the `client.chat.send()` call with `callAI(db, { memberId, feature: 'summary', messages })`
- If `result.degraded` is true, log a warning and return early (don't compress -- just skip it, existing summary stays)
- Remove the hardcoded `'deepseek/deepseek-v3.2'` model string in the send call
- Keep `estimateTokens()` function -- it's still used for pre-call context budget estimation (not for cost tracking)
- **Update context budget constants:** Change `CONTEXT_BUDGET` from 100_000 to 1_400_000 (~70% of Grok 4.1 Fast's 2M context). Update `SYSTEM_PROMPT_RESERVE` from 20_000 to 50_000 (more headroom with larger window). Keep `OUTPUT_RESERVE` at 4_000. Update the module doc comment to reference Grok 4.1 Fast instead of DeepSeek V3.2

**nudge.ts:**
- Feature tag: `'nudge'`
- memberId: available in the `sendNudge` function
- db: need to check -- `sendNudge` may need `db` added to its parameter list. Read the full function signature. The AI call is in `sendNudge` or a helper. Pass db through from the caller.
- The existing hardcoded nudge fallback string is the degradation response
- Remove `import { config } from '../../core/config.js'` if only used for OPENROUTER_API_KEY
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <done>chat.ts, memory.ts, and nudge.ts all use callAI() from shared/ai-client. No OpenRouter import or client instance remains in any of the 3 files. No hardcoded model strings. Each file handles degradation with its existing fallback. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate briefs, planning, categories, filter, ai-tags, and tagger</name>
  <files>src/modules/scheduler/briefs.ts, src/modules/scheduler/planning.ts, src/modules/checkin/ai-categories.ts, src/modules/auto-feed/filter.ts, src/modules/profile/ai-tags.ts, src/modules/resources/tagger.ts</files>
  <action>
**Apply the same migration pattern to all 6 files:**

1. Remove `import { OpenRouter } from '@openrouter/sdk'`
2. Remove the `let openrouterClient` and `getOpenRouterClient()` block
3. Add `import { callAI } from '../../shared/ai-client.js'`
4. Replace each `client.chat.send(...)` with `callAI(db, { memberId, feature, messages, responseFormat? })`
5. Handle `result.degraded` using each file's existing fallback

**File-specific details:**

**briefs.ts:**
- Feature tag: `'brief'`
- memberId: available from the brief generation context (the member being briefed)
- db: already passed to `generateBrief()`
- Existing fallback: `formatTemplateFallback(template)` -- use when `result.degraded` is true
- Remove `import { config } from '../../core/config.js'` if only used for API key (check -- config may also be used for DISCORD_GUILD_ID or other vars; if so, keep the import)

**planning.ts:**
- Feature tag: `'planning'`
- memberId: available from the planning session context
- db: already passed to the planning function
- Existing fallback: `[{ title: text, type: 'freetext' }]` -- the function that calls AI for goal extraction should return this on degradation
- The AI call extracts goals from natural language. If degraded, return the raw text as a single freetext goal

**ai-categories.ts:**
- Feature tag: `'categories'`
- memberId: **This function currently does NOT receive a memberId or db.** The `extractCategories(text)` function signature only takes text. It needs to be updated:
  - Change signature to `extractCategories(db: ExtendedPrismaClient, memberId: string, text: string)`
  - Add `import type { ExtendedPrismaClient } from '../../db/client.js'`
  - **Find the caller** (in `src/modules/checkin/commands.ts`) and update it to pass `db` and `memberId` to `extractCategories()`
  - Existing fallback: `{ categories: ['general'], goalHints: [] }`
  - Include `src/modules/checkin/commands.ts` in files modified for this task

**filter.ts:**
- Feature tag: `'filter'`
- memberId: use `'system'` -- content filtering is server-level, not tied to a member
- db: **Check if db is already passed.** The filter function likely receives db for FeedPost queries. If not, add it to the parameter and update callers.
- Existing fallback: return empty array `[]` (no unfiltered content)
- Since memberId is 'system', budget check will always pass (per ai-client implementation)

**ai-tags.ts:**
- Feature tag: `'tags'`
- memberId: **This function currently does NOT receive a memberId or db.** The `extractProfileTags(rawAnswers)` function only takes raw answers. Update:
  - Change signature to `extractProfileTags(db: ExtendedPrismaClient, memberId: string, rawAnswers: Record<string, string>)`
  - Add `import type { ExtendedPrismaClient } from '../../db/client.js'`
  - **Find the caller** (in `src/modules/profile/` or `src/modules/onboarding/`) and update it to pass `db` and `memberId`
  - Existing fallback: `fallbackExtraction(rawAnswers)` function already exists in the file
  - Include the caller file in files modified

**tagger.ts (resources/tagger.ts):**
- Feature tag: `'tagger'`
- memberId: use `'system'` -- resource tagging is server-level
- db: **Check if db is available.** If not passed, add it to the function signature and update callers
- Existing fallback: `{ topic: 'Resource', tags: [] }`

**IMPORTANT for ai-categories.ts, ai-tags.ts, and tagger.ts:**
These functions currently don't receive `db` or `memberId`. You must:
1. Update the function signatures
2. Find ALL callers by searching for the import/call
3. Update all callers to pass the new parameters
4. This is safe because these are internal functions with 1-2 callers each

After all 6 files are migrated, verify that NO file in `src/modules/` still imports from `@openrouter/sdk` -- only `src/shared/ai-client.ts` should import it.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit --pretty 2>&1 | head -40 && echo "---" && grep -r "from '@openrouter/sdk'" src/modules/ || echo "NO DIRECT OPENROUTER IMPORTS IN MODULES - GOOD"</automated>
  </verify>
  <done>All 6 files migrated to callAI(). No file in src/modules/ imports @openrouter/sdk. Only src/shared/ai-client.ts imports the SDK. All callers of ai-categories, ai-tags, and tagger updated with db and memberId parameters. TypeScript compiles clean. Zero hardcoded model strings remain in module files.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- `grep -r "from '@openrouter/sdk'" src/modules/` returns no results
- `grep -r "from '@openrouter/sdk'" src/shared/` returns exactly 1 result (ai-client.ts)
- `grep -r "getOpenRouterClient" src/modules/` returns no results
- `grep -rn "deepseek/deepseek-v3.2\|qwen/qwen3.5" src/modules/` returns no results (no hardcoded model strings)
- All 9 module files contain `import { callAI }` or `import { callAI` from shared/ai-client
</verification>

<success_criteria>
Every AI call in the entire codebase routes through `callAI()` from `src/shared/ai-client.ts`. No module file creates its own OpenRouter client instance. No module file has hardcoded model strings. Every call passes a memberId (real or 'system') and feature tag. Every call site handles `result.degraded` with its existing fallback pattern. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-infrastructure/07-02-SUMMARY.md`
</output>
