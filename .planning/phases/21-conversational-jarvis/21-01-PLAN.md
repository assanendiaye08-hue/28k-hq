---
phase: 21-conversational-jarvis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - apps/bot/src/shared/ai-types.ts
  - apps/bot/src/shared/ai-client.ts
  - apps/bot/src/modules/ai-assistant/personality.ts
autonomous: true
requirements: [JARV-04]

must_haves:
  truths:
    - "callAI accepts a tools parameter and returns tool_calls when the LLM invokes a tool"
    - "callAI returns regular content when the LLM does not invoke a tool"
    - "Commitment model exists in the DB schema with memberId, title, deadline, status"
    - "ConversationMessage has an optional topic field for topic-aware context"
    - "Jarvis system prompt is direct, factual, brief -- no filler, no fake emotions, no unsolicited advice"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Commitment model + topic field on ConversationMessage"
      contains: "model Commitment"
    - path: "apps/bot/src/shared/ai-types.ts"
      provides: "ToolCall type, tools parameter on AICallOptions, toolCalls on AICallResult"
      contains: "toolCalls"
    - path: "apps/bot/src/shared/ai-client.ts"
      provides: "Tool calling support in sendToModel and result extraction"
      contains: "tools"
    - path: "apps/bot/src/modules/ai-assistant/personality.ts"
      provides: "Rewritten coaching tone + tool awareness instructions"
      contains: "TOOL_AWARENESS_PROMPT"
  key_links:
    - from: "apps/bot/src/shared/ai-client.ts"
      to: "apps/bot/src/shared/ai-types.ts"
      via: "imports AICallOptions with tools field"
      pattern: "tools.*function"
---

<objective>
Lay the foundation for conversational Jarvis: extend the AI client to support LLM tool calling, add the Commitment DB model and topic field for context awareness, and rewrite the personality prompt for direct coaching tone with tool awareness instructions.

Purpose: Plans 02 and 03 build on this foundation -- they need tool calling in the AI client, the Commitment table for tracking, the topic field for context filtering, and the updated personality for tone.
Output: Extended ai-client.ts, updated ai-types.ts, new Commitment model in schema.prisma, topic field on ConversationMessage, rewritten personality.ts
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-conversational-jarvis/21-RESEARCH.md
@apps/bot/src/shared/ai-types.ts
@apps/bot/src/shared/ai-client.ts
@apps/bot/src/modules/ai-assistant/personality.ts
@packages/db/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AI client for tool calling and add DB schema changes</name>
  <files>
    packages/db/prisma/schema.prisma
    apps/bot/src/shared/ai-types.ts
    apps/bot/src/shared/ai-client.ts
  </files>
  <action>
1. **ai-types.ts** -- Extend the existing types to support tool calling:
   - Add a `ToolDefinition` type: `{ type: 'function'; function: { name: string; description: string; parameters: object } }`
   - Add optional `tools?: ToolDefinition[]` field to `AICallOptions`
   - Add optional `toolCalls?: Array<{ id: string; function: { name: string; arguments: string } }>` field to `AICallResult`
   - Add 'intent' to the `AIFeature` union type (for tool-calling chat calls vs regular chat)

2. **ai-client.ts** -- Extend `sendToModel` and result handling:
   - In `sendToModel`, if `options.tools` is provided, add `params.tools = options.tools` to the ChatGenerationParams. The OpenRouter SDK's `ChatGenerationParams` type may not include `tools` -- cast the params if needed: `(params as Record<string, unknown>).tools = options.tools`
   - After receiving the completion, check `completion.choices[0]?.message?.tool_calls`. If tool_calls exist and have length > 0, return an AICallResult with `content` set to `completion.choices[0]?.message?.content ?? null` (the LLM may include a conversational response alongside tool calls) and `toolCalls` populated from the tool_calls array.
   - The `tools` parameter must be threaded from the outer `callAI` function into `sendToModel`. Currently `sendToModel` is a local closure that reads from `options` -- it already has access. Just wire `options.tools` into `params` inside `sendToModel`.

3. **schema.prisma** -- Add Commitment model and topic field:
   - Add `topic String?` field to the existing `ConversationMessage` model (after the `content` field)
   - Add `CommitmentStatus` enum: `ACTIVE`, `COMPLETED`, `MISSED`, `CANCELLED`
   - Add `Commitment` model with fields: `id` (cuid), `memberId` (String), `title` (String), `deadline` (DateTime), `status` (CommitmentStatus, default ACTIVE), `sourceMessageId` (String?), `createdAt` (DateTime default now), `completedAt` (DateTime?)
   - Add relation: `member Member @relation(fields: [memberId], references: [id], onDelete: Cascade)`
   - Add indexes: `@@index([memberId, status])`, `@@index([deadline, status])`
   - Add `commitments Commitment[]` relation field to the `Member` model (in the Relations section)

After schema changes, run `npx prisma generate` from the `packages/db` directory to regenerate the client. Do NOT run `prisma db push` (that happens on VPS deployment).
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord Hustler/packages/db && npx prisma validate && npx prisma generate 2>&1 | tail -5</automated>
    <manual>Verify ai-types.ts has tools and toolCalls fields, ai-client.ts passes tools to params</manual>
  </verify>
  <done>
    - AICallOptions has optional `tools` field
    - AICallResult has optional `toolCalls` field
    - sendToModel passes tools to OpenRouter params when provided
    - Tool calls are extracted from completion response and returned in AICallResult
    - Commitment model exists in schema with memberId, title, deadline, status
    - ConversationMessage has optional topic field
    - Prisma client regenerated successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite personality for coaching tone and tool awareness</name>
  <files>
    apps/bot/src/modules/ai-assistant/personality.ts
  </files>
  <action>
1. **Replace CHARACTER_PROMPT** with a direct, situationally smart coaching tone (JARV-04):
   ```
   You are Jarvis. You operate for one person at a time inside a productivity Discord server.

   Your style:
   - Direct and factual. No pleasantries, no filler, no "That's awesome!" reactions.
   - Brief by default. Expand only when asked or when the topic demands it.
   - Never fake emotions or enthusiasm. If something is impressive, state why factually.
   - When you have data (goals, streaks, check-ins), reference it directly.
   - When you do NOT have enough info, ask a question instead of guessing.
   - Never volunteer unsolicited life advice or motivational speeches.
   - Adapt push level to their accountability setting: light = gentle, medium = direct, heavy = blunt.
   ```

2. **Replace CONVERSATION_RULES** with updated rules:
   ```
   Rules:
   - Reference past conversations naturally. Follow up on things they mentioned.
   - If they committed to something, ask about it next time.
   - Keep responses under 3 sentences unless they ask for detail.
   - No emoji floods. One emoji max per message, only if it adds clarity.
   - When referencing other members, keep it anonymous.
   - If they have a top-level goal with no sub-goals, suggest decomposition once. Don't push it.
   - When reflection data exists, make one specific forward-looking suggestion per conversation -- reference the actual insight, don't be vague.
   ```

3. **Add TOOL_AWARENESS_PROMPT** constant (new, appended to system prompt):
   ```
   You have tools for: logging check-ins, creating goals, setting reminders, tracking commitments, and starting brainstorming sessions.

   ONLY call a tool when the user clearly intends to:
   - LOG what they accomplished (check-in)
   - CREATE a new goal with a deadline
   - SET a reminder for a specific time
   - COMMIT to doing something by a deadline ("I'll have X done by Y")
   - BRAINSTORM or explore ideas on a topic

   Do NOT call tools for:
   - Casual conversation about what they did (not a check-in unless they want to log it)
   - Questions about their goals or progress
   - Discussing strategies or giving advice
   - Expressing feelings, venting, or reflecting

   When you call a tool, also include a brief conversational acknowledgment. The system appends a confirmation prompt automatically.

   TOPIC AWARENESS:
   Classify each exchange with a topic tag based on the subject matter. When the user switches topics, follow naturally. Do not bleed context from unrelated topics unless they connect them explicitly.
   ```

4. **Update `buildSystemPrompt`** to append `TOOL_AWARENESS_PROMPT` after `CONVERSATION_RULES` in the sections array.

5. Keep ALL existing helper functions (`buildProfileSection`, `buildStatsSection`, `buildActivitySection`, `buildInspirationSection`, `buildReflectionSection`) unchanged -- they provide the member data context that makes Jarvis situationally smart.
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord Hustler && npx tsc --noEmit --project apps/bot/tsconfig.json 2>&1 | head -20</automated>
    <manual>Read personality.ts and verify tone matches JARV-04: no pleasantries, factual, brief, questions when unsure</manual>
  </verify>
  <done>
    - CHARACTER_PROMPT rewritten: direct, factual, brief, no fake emotions
    - CONVERSATION_RULES updated: follow up on commitments, 3-sentence default, no emoji floods
    - TOOL_AWARENESS_PROMPT added: clear tool calling guidelines with negative examples
    - System prompt builder appends tool awareness after conversation rules
    - All existing profile/stats/activity/inspiration/reflection sections preserved
    - TypeScript compilation passes
  </done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes in packages/db
- `npx tsc --noEmit` passes for apps/bot
- ai-types.ts exports ToolDefinition type and updated AICallOptions/AICallResult
- ai-client.ts passes tools to OpenRouter and extracts tool_calls from response
- personality.ts has direct coaching tone with tool awareness prompt
- ConversationMessage model has `topic String?` field
- Commitment model exists with correct fields and indexes
</verification>

<success_criteria>
The AI client supports tool calling (tools in, toolCalls out), the DB schema has the Commitment model and topic field, and the personality prompt establishes Jarvis's direct coaching tone with clear tool calling boundaries. Plans 02 and 03 can build on this foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/21-conversational-jarvis/21-01-SUMMARY.md`
</output>
