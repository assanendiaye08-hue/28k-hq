---
phase: 13-monthly-progress-recap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/recap/constants.ts
  - src/modules/recap/aggregator.ts
  - src/modules/recap/generator.ts
  - src/modules/recap/share.ts
  - src/modules/recap/index.ts
  - src/shared/ai-types.ts
  - src/modules/scheduler/index.ts
autonomous: true
requirements: [RECAP-01, RECAP-02]

must_haves:
  truths:
    - "Member receives a monthly progress recap DM on the 1st of each month with AI-generated commentary covering check-ins, goals, timer sessions, voice hours, reflections, XP, and streaks"
    - "Recap adapts depth to available data -- rich data gets detailed narrative + suggestions, thin data gets brief encouragement"
    - "Member can react to their recap DM to share condensed highlights to #wins channel"
    - "Shared version shows key stats and one Jarvis quote -- not the full private recap"
  artifacts:
    - path: "src/modules/recap/constants.ts"
      provides: "Recap configuration, emoji trigger, channel name"
    - path: "src/modules/recap/aggregator.ts"
      provides: "Monthly data aggregation queries across all tables"
      exports: ["aggregateMonthlyData"]
    - path: "src/modules/recap/generator.ts"
      provides: "AI recap generation with adaptive depth and template fallback"
      exports: ["generateRecap"]
    - path: "src/modules/recap/share.ts"
      provides: "Condensed public recap builder and #wins poster"
      exports: ["buildPublicRecap", "shareToWins"]
    - path: "src/modules/recap/index.ts"
      provides: "Module registration with cron and reaction listener"
  key_links:
    - from: "src/modules/recap/aggregator.ts"
      to: "prisma (CheckIn, Goal, TimerSession, VoiceSession, XPTransaction, Reflection)"
      via: "db queries with month date range"
      pattern: "db\\.(checkIn|goal|timerSession|voiceSession|xPTransaction|reflection)\\."
    - from: "src/modules/recap/generator.ts"
      to: "src/shared/ai-client.ts"
      via: "callAI with feature 'recap'"
      pattern: "callAI.*feature.*recap"
    - from: "src/modules/recap/index.ts"
      to: "src/modules/recap/generator.ts"
      via: "monthly cron on 1st calls generateRecap for each member"
      pattern: "cron\\.schedule.*generateRecap|sendRecap"
    - from: "src/modules/recap/index.ts"
      to: "src/modules/recap/share.ts"
      via: "messageReactionAdd listener triggers shareToWins"
      pattern: "messageReactionAdd.*shareToWins"
---

<objective>
Build the monthly progress recap module -- AI-generated monthly summary DM with share-to-#wins via reaction.

Purpose: Members accumulate data daily but have no periodic "look how far you've come" moment. The monthly recap closes that loop -- a Jarvis-narrated summary of patterns, growth, and suggestions drawn from all data sources (check-ins, goals, timers, voice, reflections, XP). The share-to-#wins feature adds social proof.

Output: `src/modules/recap/` module (5 files) + AIFeature type update + scheduler cron registration.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-monthly-progress-recap/13-CONTEXT.md
@src/modules/scheduler/index.ts (cron registration pattern)
@src/modules/scheduler/briefs.ts (data aggregation + AI generation pattern)
@src/shared/ai-client.ts (callAI interface)
@src/shared/ai-types.ts (AIFeature type to extend)
@src/shared/constants.ts (BRAND_COLORS, SERVER_CATEGORIES)
@src/modules/notification-router/router.ts (deliverNotification pattern)
@src/modules/wins-lessons/constants.ts (WINS_CHANNEL_NAME)
@src/shared/embeds.ts (embed builder patterns)
@prisma/schema.prisma (data models to query)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recap module -- aggregator, AI generator, constants</name>
  <files>
    src/modules/recap/constants.ts
    src/modules/recap/aggregator.ts
    src/modules/recap/generator.ts
    src/shared/ai-types.ts
  </files>
  <action>
**1. Add 'recap' to AIFeature type** in `src/shared/ai-types.ts`:
Add `| 'recap'` to the AIFeature union type (after 'reflection').

**2. Create `src/modules/recap/constants.ts`:**
```
RECAP_EMOJI = 'trophy' (Unicode trophy emoji)
RECAP_CRON = '0 10 1 * *' (10:00 AM UTC on 1st of each month)
MIN_DATA_THRESHOLD = 3 (minimum check-ins to warrant a detailed recap vs brief encouragement)
```

**3. Create `src/modules/recap/aggregator.ts`:**

Export `aggregateMonthlyData(db, memberId, monthStart, monthEnd)` that queries:
- **Check-ins**: count, streak info (from Member.currentStreak, longestStreak), categories frequency (group by category across all check-ins in range)
- **Goals**: completed count, active count, new goals set, goals by timeframe if hierarchy exists
- **Timer sessions**: total sessions, total worked ms, total break ms, average session length, top focus areas (group by focus text)
- **Voice sessions**: total sessions, total minutes
- **XP**: total earned in month (sum XPTransaction.amount), breakdown by source
- **Reflections**: count by type (daily/weekly/monthly), any insights (last 3 non-null insights)

Return a typed `MonthlyRecapData` interface with all fields. Use `Promise.all` for parallel queries (same pattern as `getCommunityPulse` in briefs.ts). The monthStart/monthEnd should be computed from the previous month (recap runs on 1st, covers prior month).

**4. Create `src/modules/recap/generator.ts`:**

Export `generateRecap(db, memberId)` and `sendRecap(client, db, memberId)`.

`generateRecap`:
- Compute previous month's date range (1st 00:00:00 to last day 23:59:59) using date-fns
- Call `aggregateMonthlyData` to get stats
- Build AI prompt with adaptive depth instruction:
  - System prompt: Use `buildSystemPrompt(db, memberId)` from personality.ts + recap addendum
  - Recap addendum: "Write a monthly progress recap for this member. Cover their patterns, growth, and areas to improve. Be specific -- reference actual numbers. If data is rich (many check-ins, goals, sessions), write a detailed narrative (4-8 sentences) with 1-2 forward-looking suggestions grounded in evidence. If data is thin (few check-ins, minimal activity), keep it brief and encouraging (2-3 sentences) -- don't make up patterns from insufficient data. Never force suggestions when data is insufficient. Include a memorable one-liner that captures their month."
  - User message: JSON of MonthlyRecapData
- Call `callAI(db, { memberId, feature: 'recap', messages })`
- If degraded, use template fallback (format stats as clean text, no AI personality)
- Return `{ narrative: string, stats: MonthlyRecapData, jarvisQuote: string }` -- extract the one-liner from AI response as jarvisQuote (instruct AI to wrap it in quotes)

`sendRecap`:
- Call generateRecap
- Build a rich embed (BRAND_COLORS.primary amber/gold):
  - Title: "Monthly Recap -- {Month Name} {Year}"
  - Description: AI narrative
  - Fields (inline): XP Earned, Check-ins, Goals Completed, Focus Hours (totalWorkedMs -> hours), Voice Hours, Reflections count
  - Only show fields with non-zero values (adaptive -- don't show "Timer Hours: 0")
  - Footer: "React with {RECAP_EMOJI} to share highlights to #wins"
- Deliver via `deliverNotification(client, db, memberId, 'general', { embeds: [embed] })`
- Return the sent message (for reaction tracking) -- use `deliverToPrivateSpace` directly (not deliverNotification) since we need the Message object back. Modify approach: send DM directly to the member's first linked DiscordAccount, similar to how briefs.ts works but returning the Message. Store the message ID and memberId in an in-memory Map `pendingRecaps` for reaction tracking.

Follow patterns from briefs.ts for data fetching, AI calling, embed construction, and delivery. Use winston logger tagged `[recap]`.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify aggregator queries cover all 6 data sources, generator produces embed with adaptive fields</manual>
  </verify>
  <done>
    - MonthlyRecapData interface aggregates check-ins, goals, timers, voice, XP, reflections for a month
    - generateRecap calls AI with adaptive depth instruction and falls back to template
    - sendRecap builds branded embed with non-zero stat fields and delivers via DM
    - AIFeature type includes 'recap'
    - pendingRecaps Map tracks sent recap message IDs for reaction-based sharing
  </done>
</task>

<task type="auto">
  <name>Task 2: Share-to-#wins reaction listener, module registration, scheduler cron</name>
  <files>
    src/modules/recap/share.ts
    src/modules/recap/index.ts
    src/modules/scheduler/index.ts
  </files>
  <action>
**1. Create `src/modules/recap/share.ts`:**

Export `buildPublicRecap(stats: MonthlyRecapData, jarvisQuote: string, displayName: string)`:
- Build a condensed embed for #wins (BRAND_COLORS.primary):
  - Title: "{displayName}'s Month in Review"
  - Description: The jarvisQuote (one Jarvis quote from the AI recap, in italics)
  - Fields (inline, only non-zero): XP Earned, Check-ins, Goals Completed, Focus Hours, Voice Hours
  - NO personal insights, reflection details, or suggestions -- just stats + quote
  - Footer: "Monthly Recap | Keep grinding"

Export `shareToWins(client, db, memberId, stats, jarvisQuote)`:
- Look up member's displayName from db
- Call buildPublicRecap to get the embed
- Find #wins channel in the guild (use `WINS_CHANNEL_NAME` from wins-lessons/constants.ts or define locally as 'wins')
- Post the embed to #wins
- Log success

**2. Create `src/modules/recap/index.ts`:**

Module structure following the auto-discovered pattern (default export Module object):

`register(ctx)`:
- Import `sendRecap` from generator.ts and `pendingRecaps` Map
- Import `shareToWins` from share.ts
- Import `RECAP_EMOJI`, `RECAP_CRON` from constants.ts

- **Reaction listener** (`client.on('messageReactionAdd', ...)`):
  - Check if the reaction emoji matches RECAP_EMOJI
  - Check if the message ID is in pendingRecaps Map
  - If yes: look up the memberId from pendingRecaps, call shareToWins with the stored stats/quote
  - Remove from pendingRecaps after sharing (one-time share)
  - Ignore reactions from bots

- Note: The monthly cron job will be registered in the scheduler module (Task 2 step 3), not in this module's register function, to follow the established pattern where global crons live in scheduler/index.ts.

- Log module registration

**3. Update `src/modules/scheduler/index.ts`:**

Add a new global cron schedule for monthly recap sweep (following the pattern of the monthly reflection sweep on line 261):

```typescript
// Schedule monthly recap sweep on the 1st at 10:00 UTC
cron.schedule('0 10 1 * *', async () => {
  try {
    const members = await db.member.findMany({
      where: { schedule: { isNot: null } },
      select: { id: true },
    });

    for (const member of members) {
      try {
        await sendRecap(client, db, member.id);
      } catch (error) {
        logger.error(`Monthly recap failed for ${member.id}: ${String(error)}`);
      }
    }

    logger.info(`Monthly recap sweep completed: ${members.length} members`);
  } catch (error) {
    logger.error(`Monthly recap sweep failed: ${String(error)}`);
  }
}, { name: 'monthly-recap-sweep' });
```

Import `sendRecap` from `../recap/generator.js` at the top of scheduler/index.ts.

Add a log line: `logger.info('Scheduled monthly recap sweep (1st at 10:00 UTC)');`

**Key implementation details:**
- The pendingRecaps Map must be exported from generator.ts (or a shared state file) so both the generator (to populate on send) and the index.ts reaction listener (to check) can access it. Store `{ memberId, stats, jarvisQuote }` keyed by Discord message ID.
- For the reaction listener, use partials to handle uncached messages: check if `reaction.partial` and fetch if needed, same for user.
- The recap module's index.ts only handles the reaction listener. The cron lives in scheduler to follow existing patterns.
- Use the trophy emoji: check both `reaction.emoji.name === 'trophy'` (custom) and Unicode match.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify scheduler/index.ts has the monthly recap cron, recap module has reaction listener</manual>
  </verify>
  <done>
    - shareToWins posts condensed stats + Jarvis quote to #wins (no private details)
    - Reaction on recap DM with trophy emoji triggers share flow
    - Monthly cron fires on 1st at 10:00 UTC for all members with schedules
    - Module auto-discovered by module-loader (src/modules/recap/index.ts default export)
    - pendingRecaps Map bridges generator and reaction listener
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Recap module is auto-discovered (src/modules/recap/index.ts has default Module export)
3. Scheduler has monthly-recap-sweep cron entry alongside existing crons
4. AIFeature type includes 'recap'
5. aggregateMonthlyData queries all 6 data sources: CheckIn, Goal, TimerSession, VoiceSession, XPTransaction, Reflection
6. generateRecap calls callAI with adaptive depth instruction and has template fallback
7. sendRecap delivers embed via DM with stat fields and trophy reaction prompt
8. shareToWins posts condensed version to #wins (stats + quote, no private details)
9. Reaction listener checks pendingRecaps Map and fires shareToWins on trophy react
</verification>

<success_criteria>
- Member receives monthly recap DM with AI narrative covering patterns, growth, suggestions
- Recap adapts: rich data = detailed narrative + suggestions, thin data = brief encouragement
- Member reacts with trophy to share condensed highlights to #wins
- Public version shows stats + one Jarvis quote (no personal details)
- Monthly cron on 1st at 10:00 UTC processes all members with schedules
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-monthly-progress-recap/13-01-SUMMARY.md`
</output>
