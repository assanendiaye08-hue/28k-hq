---
phase: 18-flowmodoro-goals-editing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/stores/goals-store.ts
  - apps/desktop/src/components/goals/GoalNode.tsx
  - apps/desktop/src/components/goals/CreateGoalForm.tsx
  - apps/desktop/src/pages/GoalsPage.tsx
autonomous: true
requirements: [GOAL-03, GOAL-04, GOAL-05]

must_haves:
  truths:
    - "User can click Add Goal button on GoalsPage and fill out a creation form"
    - "User can set title, type (measurable/freetext), deadline, timeframe, optional parent, and target value"
    - "Created goal appears in the goal tree after submission"
    - "User can update progress on a measurable goal directly from the tree"
    - "User can mark any active goal as complete from the tree"
    - "Completing a goal shows XP awarded feedback"
  artifacts:
    - path: "apps/desktop/src/stores/goals-store.ts"
      provides: "createGoal, updateProgress, completeGoal actions"
      exports: ["useGoalsStore"]
    - path: "apps/desktop/src/components/goals/CreateGoalForm.tsx"
      provides: "Goal creation form with all required fields"
      min_lines: 50
    - path: "apps/desktop/src/components/goals/GoalNode.tsx"
      provides: "Inline action buttons for progress update and complete"
      contains: "completeGoal"
    - path: "apps/desktop/src/pages/GoalsPage.tsx"
      provides: "Add Goal button that toggles CreateGoalForm"
      contains: "CreateGoalForm"
  key_links:
    - from: "apps/desktop/src/stores/goals-store.ts"
      to: "POST /goals API"
      via: "apiFetch in createGoal action"
      pattern: "apiFetch.*goals.*POST"
    - from: "apps/desktop/src/stores/goals-store.ts"
      to: "PATCH /goals/:id API"
      via: "apiFetch in updateProgress and completeGoal"
      pattern: "apiFetch.*goals.*PATCH"
    - from: "apps/desktop/src/components/goals/GoalNode.tsx"
      to: "apps/desktop/src/stores/goals-store.ts"
      via: "calls completeGoal and updateProgress actions"
      pattern: "completeGoal|updateProgress"
---

<objective>
Add goal CRUD operations (create, update progress, complete) to the desktop app's goals page.

Purpose: Users can currently only view goals in the desktop app. This adds the ability to create goals, track progress on measurable goals, and mark goals complete -- all calling existing API endpoints that handle validation, depth enforcement, and XP awards.

Output: Goals store has mutation actions, GoalNode has inline action buttons, CreateGoalForm exists as a slide-down panel, GoalsPage has an Add Goal button.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-flowmodoro-goals-editing/18-RESEARCH.md
@apps/desktop/src/stores/goals-store.ts
@apps/desktop/src/components/goals/GoalNode.tsx
@apps/desktop/src/components/goals/GoalTree.tsx
@apps/desktop/src/pages/GoalsPage.tsx
@apps/api/src/routes/goals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mutation actions to goals store and create goal form component</name>
  <files>
    apps/desktop/src/stores/goals-store.ts
    apps/desktop/src/components/goals/CreateGoalForm.tsx
  </files>
  <action>
    **goals-store.ts:**
    1. Add `isSubmitting: boolean` field (default false) for loading states on mutations.
    2. Add `createGoal` action: accepts `{ title: string, type: 'MEASURABLE' | 'FREETEXT', deadline: string, timeframe?: string, targetValue?: number, unit?: string, parentId?: string, description?: string }`. Sets isSubmitting true, calls `apiFetch('/goals', { method: 'POST', ... })`, then calls `fetchGoals()` to refresh the tree, sets isSubmitting false. On error, sets isSubmitting false and throws (caller handles).
    3. Add `updateProgress` action: accepts `(goalId: string, currentValue: number)`. Calls `apiFetch('/goals/${goalId}', { method: 'PATCH', body: { currentValue } })`. Then calls `fetchGoals()` to refresh. Returns the xpResult if present.
    4. Add `completeGoal` action: accepts `(goalId: string)`. Calls `apiFetch('/goals/${goalId}', { method: 'PATCH', body: { status: 'COMPLETED' } })`. Then calls `fetchGoals()`. Returns `{ xpAwarded: number }` from the response's `xpResult.xpAwarded` field.

    **CreateGoalForm.tsx:**
    5. Create a new component that renders as a slide-down panel (not a modal).
    6. Props: `onClose: () => void`, `parentGoals: Goal[]` (for parent selector).
    7. Form fields:
       - **Title**: text input, required, max 200 chars
       - **Type**: two-button toggle (Measurable / Freetext), default Freetext
       - **Target value + Unit**: only shown when type is MEASURABLE. Number input for target, text input for unit (e.g., "pages", "hours", "commits")
       - **Timeframe**: select dropdown with options: Yearly, Quarterly, Monthly, Weekly (maps to uppercase enum values)
       - **Deadline**: date input (required). Default to a reasonable date based on selected timeframe (end of current week/month/quarter/year)
       - **Parent goal** (optional): select dropdown showing active goals at depth < 3 from `parentGoals` prop. Display as "-- None --" + flattened goal titles with depth indentation
       - **Description** (optional): textarea, max 500 chars
    8. Submit button: calls `useGoalsStore.getState().createGoal(data)`. Show loading state (disabled button, spinner). On success call `onClose()`. On error show error message below form.
    9. Style with same dark+gold theme: `bg-surface-2` background, `border-white/5` borders, `rounded-xl` inputs, `bg-brand` submit button. Match TimerSetup styling for consistency.
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord\ Hustler && npx tsc --noEmit --project apps/desktop/tsconfig.json 2>&1 | tail -20</automated>
    <manual>Verify CreateGoalForm.tsx renders with all fields, goals store has three new actions</manual>
  </verify>
  <done>
    Goals store has createGoal, updateProgress, and completeGoal actions that call existing API endpoints and refresh the goal list. CreateGoalForm component exists with all required fields (title, type, target/unit for measurable, timeframe, deadline, optional parent, optional description). TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inline actions to GoalNode and wire CreateGoalForm into GoalsPage</name>
  <files>
    apps/desktop/src/components/goals/GoalNode.tsx
    apps/desktop/src/pages/GoalsPage.tsx
  </files>
  <action>
    **GoalNode.tsx:**
    1. Import `updateProgress` and `completeGoal` from goals store.
    2. Add local state: `isUpdating: boolean`, `showProgressInput: boolean`, `progressValue: number` (initialized to goal.currentValue).
    3. For ACTIVE + MEASURABLE goals: add a small "+" button next to the progress display. Clicking it toggles `showProgressInput` which shows an inline number input + "Save" button. Save calls `updateProgress(goal.id, progressValue)` with loading state.
    4. For all ACTIVE goals: add a small checkmark/complete button (inline SVG) that appears on hover (or always visible as a subtle icon). Clicking calls `completeGoal(goal.id)`. Show a brief toast or inline "+X XP" feedback after completion using the returned xpAwarded value. Use a temporary state to show the XP text for 2 seconds then fade.
    5. Disable action buttons while `isUpdating` is true (prevent double-clicks).
    6. Do NOT show action buttons on COMPLETED, MISSED, or EXTENDED goals.
    7. Keep existing expand/collapse, progress bar, status indicator, timeframe badge functionality unchanged.

    **GoalsPage.tsx:**
    8. Import CreateGoalForm.
    9. Add `showCreateForm: boolean` state (default false).
    10. Add "Add Goal" button next to the page title (or in the filter bar area). Style: `bg-brand text-surface-base rounded-lg px-4 py-1.5 text-sm font-medium`. Uses a "+" icon (inline SVG).
    11. When `showCreateForm` is true, render `<CreateGoalForm onClose={() => setShowCreateForm(false)} parentGoals={flattenedActiveGoals} />` above the GoalTree card. The `parentGoals` prop should be a flattened list of all active goals at depth < 3 extracted from the goals tree (recursive flatten helper).
    12. Animate the form slide-down with a simple CSS transition or Tailwind `animate-fade-in`.
    13. Update the empty state text in GoalTree: change "Create goals in Discord using /goal or ask Jarvis" to "Click 'Add Goal' above to create your first goal".
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord\ Hustler && npx tsc --noEmit --project apps/desktop/tsconfig.json 2>&1 | tail -20</automated>
    <manual>Start app, navigate to Goals page. Verify: (1) Add Goal button visible, (2) Clicking it shows create form, (3) Create a goal and it appears in tree, (4) Measurable goals show progress update button, (5) Complete button works and shows XP feedback.</manual>
  </verify>
  <done>
    GoalNode has inline progress update (+) and complete (checkmark) buttons for active goals. GoalsPage has an Add Goal button that toggles the CreateGoalForm panel. Creating a goal refreshes the tree. Updating progress and completing goals calls the API and refreshes. XP feedback shown on completion. Empty state text updated. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit --project apps/desktop/tsconfig.json`
2. Goal creation flow: Add Goal -> fill form -> submit -> goal appears in tree
3. Progress update: click + on measurable goal -> enter value -> save -> progress bar updates
4. Goal completion: click checkmark -> goal status changes to COMPLETED -> XP shown
5. Error handling: API errors show inline message, don't crash
6. Existing goal tree rendering unchanged for non-interactive use
</verification>

<success_criteria>
- Add Goal button visible on GoalsPage, opens create form panel
- Create form has all fields: title, type toggle, target/unit (measurable only), timeframe, deadline, parent, description
- Created goal appears in tree after refresh
- Measurable goals have inline progress update capability
- Any active goal can be marked complete with XP feedback
- All mutations call existing API endpoints (POST /goals, PATCH /goals/:id)
- TypeScript compiles clean, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-flowmodoro-goals-editing/18-02-SUMMARY.md`
</output>
