---
phase: 18-flowmodoro-goals-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/stores/timer-store.ts
  - apps/desktop/src/hooks/use-timer-tick.ts
  - apps/desktop/src/lib/timer-tray.ts
  - apps/desktop/src/lib/timer-persistence.ts
  - apps/desktop/src/components/timer/TimerSetup.tsx
  - apps/desktop/src/components/timer/TimerDisplay.tsx
  - apps/desktop/src/pages/TimerPage.tsx
  - apps/desktop/src/__tests__/timer-store.test.ts
autonomous: true
requirements: [FLW-01, FLW-02, FLW-03, FLW-04, FLW-05, FLW-06]

must_haves:
  truths:
    - "User can toggle between Pomodoro and Flowmodoro mode on the setup form"
    - "In flowmodoro mode, user sees only focus field and break ratio (no work/break duration, sessions, auto-start)"
    - "Flowmodoro timer counts UP showing elapsed time in the app and menu bar"
    - "When user stops flowmodoro work, a transition screen shows calculated break duration"
    - "Flowmodoro break counts DOWN like pomodoro break"
    - "Flowmodoro session syncs to API with mode PROPORTIONAL on stop"
    - "Timer state persists and restores correctly for both modes"
  artifacts:
    - path: "apps/desktop/src/stores/timer-store.ts"
      provides: "timerMode field, flowmodoro start/stop/completePhase logic, breakRatio config"
      contains: "timerMode"
    - path: "apps/desktop/src/hooks/use-timer-tick.ts"
      provides: "Count-up tick for flowmodoro work, count-down for flowmodoro break"
      contains: "elapsedMs"
    - path: "apps/desktop/src/lib/timer-tray.ts"
      provides: "Elapsed time display in menu bar for flowmodoro"
      contains: "updateTrayTitleElapsed"
    - path: "apps/desktop/src/lib/timer-persistence.ts"
      provides: "timerMode and breakRatio in SavedTimerState"
      contains: "timerMode"
    - path: "apps/desktop/src/components/timer/TimerSetup.tsx"
      provides: "Mode toggle (Pomodoro/Flowmodoro), conditional form fields"
      contains: "flowmodoro"
    - path: "apps/desktop/src/components/timer/TimerDisplay.tsx"
      provides: "Mode-aware display (count-up for flow work, count-down for flow break)"
      contains: "timerMode"
  key_links:
    - from: "apps/desktop/src/stores/timer-store.ts"
      to: "/timer API"
      via: "POST with mode PROPORTIONAL and breakRatio"
      pattern: "mode.*PROPORTIONAL"
    - from: "apps/desktop/src/hooks/use-timer-tick.ts"
      to: "apps/desktop/src/stores/timer-store.ts"
      via: "reads timerMode to branch tick behavior"
      pattern: "timerMode"
    - from: "apps/desktop/src/components/timer/TimerSetup.tsx"
      to: "apps/desktop/src/stores/timer-store.ts"
      via: "passes timerMode in StartConfig"
      pattern: "timerMode"
    - from: "apps/desktop/src/lib/timer-persistence.ts"
      to: "apps/desktop/src/stores/timer-store.ts"
      via: "SavedTimerState includes timerMode and breakRatio"
      pattern: "timerMode.*breakRatio"
---

<objective>
Add flowmodoro (count-up) timer mode to the existing pomodoro timer.

Purpose: Flowmodoro lets users work without a fixed duration -- the timer counts up, and when they stop, break time is calculated as workTime / breakRatio. This complements the structured pomodoro mode for users who prefer flow-state work.

Output: Timer store, tick hook, tray helper, persistence, setup form, and display all support both pomodoro and flowmodoro modes. Existing pomodoro behavior unchanged.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-flowmodoro-goals-editing/18-RESEARCH.md
@.planning/phases/17-pomodoro-timer/17-03-SUMMARY.md
@apps/desktop/src/stores/timer-store.ts
@apps/desktop/src/hooks/use-timer-tick.ts
@apps/desktop/src/lib/timer-tray.ts
@apps/desktop/src/lib/timer-persistence.ts
@apps/desktop/src/components/timer/TimerSetup.tsx
@apps/desktop/src/components/timer/TimerDisplay.tsx
@apps/desktop/src/pages/TimerPage.tsx
@packages/shared/src/timer-constants.ts
@apps/api/src/routes/timer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend timer store, persistence, tray, and tick hook for flowmodoro mode</name>
  <files>
    apps/desktop/src/stores/timer-store.ts
    apps/desktop/src/hooks/use-timer-tick.ts
    apps/desktop/src/lib/timer-tray.ts
    apps/desktop/src/lib/timer-persistence.ts
    apps/desktop/src/__tests__/timer-store.test.ts
  </files>
  <action>
    **timer-store.ts:**
    1. Add `timerMode: 'pomodoro' | 'flowmodoro'` to TimerState (default: 'pomodoro')
    2. Add `breakRatio: number` to TimerState (default: TIMER_DEFAULTS.defaultBreakRatio = 5)
    3. Add `timerMode` and `breakRatio` to StartConfig interface (both optional, default pomodoro/5)
    4. In `start()`: if `config.timerMode === 'flowmodoro'`, set `phaseDurationMs: 0` (not used for count-up), `timerMode: 'flowmodoro'`, `breakRatio: config.breakRatio ?? TIMER_DEFAULTS.defaultBreakRatio`. API call uses `mode: 'PROPORTIONAL'` and includes `breakRatio`. For pomodoro mode, behavior unchanged (set `timerMode: 'pomodoro'`).
    5. Add `getElapsedMs()` computed: returns `phaseStartedAt ? Date.now() - phaseStartedAt : 0`
    6. In `completePhase()`: this is NOT called during flowmodoro work phase (count-up has no completion). Only called for flowmodoro break (which counts down). When `timerMode === 'flowmodoro'` and `phase === 'on_break'`: accumulate totalBreakMs, show transition with `transitionType: 'session_complete'` (flowmodoro has no multi-session concept), call stop API with XP.
    7. Add `stopFlowWork()` action (or handle in `stop()`): When `timerMode === 'flowmodoro'` and `phase === 'working'`: calculate elapsed, accumulate totalWorkedMs, calculate `breakDurationMs = Math.round(totalWorkedMs / breakRatio)`, set `phase: 'transition'`, `transitionType: 'work_done'`, `phaseDurationMs: breakDurationMs`. This transition offers "Start Break" or "Skip & Done".
    8. Modify `stop()`: if `timerMode === 'flowmodoro'` and `phase === 'working'`, instead of going idle, trigger the work_done transition with calculated break. If `phase === 'on_break'` or `phase === 'transition'`, go idle and sync API.
    9. Add `transitionToFlowBreak()` action: sets `phase: 'on_break'`, `phaseStartedAt: Date.now()`, `phaseDurationMs` to the calculated break from transition state. Clears transitionType.
    10. Add `skipFlowBreak()` action: goes directly to idle, clears state, syncs API with stop action.
    11. Update `buildSaveState()` to include `timerMode` and `breakRatio`.
    12. Update `restore()` to handle `timerMode` and `breakRatio` from saved state (default 'pomodoro'/5 for missing fields -- backward compat).
    13. Update `clearLastStopResult()` and `stop()` reset blocks to include `timerMode: 'pomodoro'`, `breakRatio: TIMER_DEFAULTS.defaultBreakRatio`.

    **timer-persistence.ts:**
    14. Add `timerMode?: string` and `breakRatio?: number` to SavedTimerState interface (optional for backward compat with existing saved states).

    **timer-tray.ts:**
    15. Add `updateTrayTitleElapsed(elapsedMs: number)` function that formats elapsed time the same way (H:MM:SS or MM:SS) but with a different tooltip: `28K HQ - Flow: MM:SS`. Uses same formatting logic as updateTrayTitle but doesn't clear on 0 (elapsed starts at 0).

    **use-timer-tick.ts:**
    16. Add `timerMode` selector from store.
    17. Add `getElapsedMs` selector from store.
    18. Export `elapsedMs` alongside `remainingMs` from the hook return: `{ remainingMs, elapsedMs, formattedTime }`.
    19. In the effect: when `timerMode === 'flowmodoro'` and `phase === 'working'`: tick updates `elapsedMs` via `getElapsedMs()`, calls `updateTrayTitleElapsed(elapsed)`, does NOT check for completion (user stops manually). For `phase === 'on_break'` (flowmodoro break is countdown): same as pomodoro -- use `getRemainingMs()`, `updateTrayTitle()`, check `remaining <= 0` -> `completePhase()`.
    20. Update `formatTime()` to handle the elapsed display (same format, no change needed since it just formats ms).

    **timer-store.test.ts:**
    21. Add tests for flowmodoro mode:
        - `start() in flowmodoro mode sets timerMode and breakRatio`
        - `stop() during flowmodoro work triggers transition with calculated break`
        - `transitionToFlowBreak() starts countdown break`
        - `skipFlowBreak() goes to idle`
        - `getElapsedMs() returns elapsed time from phaseStartedAt`
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord\ Hustler && npx vitest run apps/desktop/src/__tests__/timer-store.test.ts 2>&1 | tail -20</automated>
    <manual>Verify timer-store.ts compiles without errors, all existing pomodoro tests still pass alongside new flowmodoro tests</manual>
  </verify>
  <done>
    Timer store supports both pomodoro and flowmodoro modes. Flowmodoro start sets mode PROPORTIONAL, elapsed time is computable, stopping work calculates break from ratio, break counts down. Persistence saves/restores timerMode and breakRatio. Tray shows elapsed time for flowmodoro work. Tick hook branches on timerMode. All tests pass (existing 7 + new flowmodoro tests).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mode toggle to TimerSetup and mode-aware TimerDisplay</name>
  <files>
    apps/desktop/src/components/timer/TimerSetup.tsx
    apps/desktop/src/components/timer/TimerDisplay.tsx
    apps/desktop/src/pages/TimerPage.tsx
  </files>
  <action>
    **TimerSetup.tsx:**
    1. Add `timerMode` state: `'pomodoro' | 'flowmodoro'`, default 'pomodoro'.
    2. Add a segmented control / tab toggle at the top of the form (above Focus field): two buttons "Pomodoro" and "Flowmodoro" with the active one highlighted in brand color, inactive in surface-2. Same styling pattern as the timeframe filter pills on GoalsPage.
    3. Add `breakRatio` state (default: TIMER_DEFAULTS.defaultBreakRatio = 5), shown only in flowmodoro mode.
    4. When `timerMode === 'flowmodoro'`: HIDE work duration, break duration, long break, sessions, auto-start toggles. SHOW only: focus field (same as pomodoro) + break ratio input. The break ratio input should use hours+minutes style but simpler: a single number input labeled "Break ratio" with helper text "Break = work time / ratio (e.g., 50 min work -> 10 min break at 5:1)". Range: 1-10, default 5.
    5. When `timerMode === 'pomodoro'`: show all existing fields (unchanged behavior).
    6. In `handleStart()`: pass `timerMode` and `breakRatio` (if flowmodoro) to `start()`.

    **TimerDisplay.tsx:**
    7. Read `timerMode` from store.
    8. When `timerMode === 'flowmodoro'` and `phase === 'working'`: use `elapsedMs` from useTimerTick instead of `remainingMs`. Show elapsed time counting UP. ProgressRing: either hide it or show a fill-up ring (reverse progress). Phase label: "Flow" in brand color. No SessionDots (flowmodoro has no session concept).
    9. When `timerMode === 'flowmodoro'` and `phase === 'on_break'`: show countdown as normal (same as pomodoro break). Phase label: "Break". No SessionDots.
    10. When `timerMode === 'flowmodoro'` and `phase === 'transition'`: handle transitions. For `work_done`: show calculated break duration offer with "Start Break" (calls `transitionToFlowBreak()`) and "Done" (calls `skipFlowBreak()`). For `session_complete`: show XP result like pomodoro.
    11. TimerControls: in flowmodoro work phase, the "Stop" button triggers the break calculation transition (calls `stop()` which now handles flowmodoro differently). Pause/Resume work the same as pomodoro.

    **TimerPage.tsx:**
    12. Update restore logic: when saved state has `phaseDurationMs === 0` and `timerMode === 'flowmodoro'`, still restore (currently it skips restore when phaseDurationMs is 0).
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord\ Hustler && npx tsc --noEmit --project apps/desktop/tsconfig.json 2>&1 | tail -20</automated>
    <manual>Start app with `pnpm dev --filter desktop`. Verify: (1) Mode toggle visible on setup form, (2) Switching to Flowmodoro hides duration/session fields and shows ratio, (3) Starting flowmodoro shows count-up timer, (4) Stopping shows break offer, (5) Starting break counts down, (6) Pomodoro mode still works identically to before.</manual>
  </verify>
  <done>
    TimerSetup has a Pomodoro/Flowmodoro mode toggle. Flowmodoro mode shows simplified form (focus + ratio only). TimerDisplay shows count-up for flowmodoro work, countdown for flowmodoro break, and appropriate transitions. TimerPage restores flowmodoro sessions correctly. All TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit --project apps/desktop/tsconfig.json`
2. Timer store tests pass: `npx vitest run apps/desktop/src/__tests__/timer-store.test.ts`
3. Existing pomodoro behavior unchanged (no regression)
4. Flowmodoro start -> count-up -> stop -> break offer -> break countdown -> XP -> idle flow works
5. Tray shows elapsed time during flowmodoro work, countdown during flowmodoro break
6. Persistence round-trips timerMode and breakRatio correctly
</verification>

<success_criteria>
- Flowmodoro mode selectable from setup form with simplified config (focus + ratio)
- Count-up timer displays elapsed time in app and menu bar
- Stopping work shows calculated break duration transition
- Break counts down after user accepts
- Session syncs to API with mode PROPORTIONAL
- All existing pomodoro tests + new flowmodoro tests pass
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/18-flowmodoro-goals-editing/18-01-SUMMARY.md`
</output>
