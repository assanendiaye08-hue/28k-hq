---
phase: 08-inspiration-system
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/modules/ai-assistant/personality.ts
  - src/modules/ai-assistant/memory.ts
autonomous: true
requirements: [INSP-02, INSP-03]

must_haves:
  truths:
    - "Jarvis's system prompt includes the member's inspirations with names and context"
    - "Member can ask Jarvis 'what would [inspiration] do?' and get a response grounded in that person's known philosophy"
    - "Jarvis references inspirations naturally in morning briefs when contextually relevant"
    - "Jarvis references inspirations in nudges when contextually relevant"
    - "Inspirations are never forced into every message -- only when relevant"
    - "Inspirations are protected data in tiered memory -- never trimmed or compressed"
  artifacts:
    - path: "src/modules/ai-assistant/personality.ts"
      provides: "Inspiration section in buildSystemPrompt with guidance for natural referencing and 'what would X do' handling"
      contains: "INSPIRATIONS"
    - path: "src/modules/ai-assistant/memory.ts"
      provides: "Inspirations loaded as protected data in buildMemberContext"
      contains: "inspirations"
  key_links:
    - from: "src/modules/ai-assistant/personality.ts"
      to: "prisma/schema.prisma"
      via: "db.inspiration.findMany in buildSystemPrompt"
      pattern: "db\\.inspiration\\.findMany"
    - from: "src/modules/ai-assistant/memory.ts"
      to: "prisma/schema.prisma"
      via: "db.inspiration.findMany in assembleContext member query or buildMemberContext"
      pattern: "inspiration"
    - from: "src/modules/ai-assistant/personality.ts"
      to: "src/modules/scheduler/briefs.ts"
      via: "briefs.ts already calls buildSystemPrompt() -- inherits inspiration context automatically"
      pattern: "buildSystemPrompt"
    - from: "src/modules/ai-assistant/personality.ts"
      to: "src/modules/ai-assistant/nudge.ts"
      via: "nudge.ts already calls buildSystemPrompt() -- inherits inspiration context automatically"
      pattern: "buildSystemPrompt"
---

<objective>
Enrich Jarvis's AI personality and memory system with inspiration context so that Jarvis can naturally reference member inspirations in conversations, briefs, and nudges, and handle "what would [person] do?" queries.

Purpose: Make the inspiration data actionable -- stored inspirations become part of Jarvis's understanding of each member, enabling personalized AI interactions grounded in the member's role models.
Output: Updated personality.ts and memory.ts that include inspiration data in system prompts and protected memory context.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-inspiration-system/08-01-SUMMARY.md
@src/modules/ai-assistant/personality.ts
@src/modules/ai-assistant/memory.ts
@src/modules/ai-assistant/chat.ts
@src/modules/ai-assistant/nudge.ts
@src/modules/scheduler/briefs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inspiration context to system prompt in personality.ts</name>
  <files>src/modules/ai-assistant/personality.ts</files>
  <action>
Modify buildSystemPrompt() in personality.ts to include the member's inspirations:

1. Add an `inspirations` query to the existing db.member.findUniqueOrThrow include block:
   ```
   inspirations: {
     orderBy: { createdAt: 'asc' },
     select: { name: true, context: true },
   },
   ```

2. Create a new `buildInspirationSection()` function that:
   - Takes the inspirations array (name, context)
   - If empty, returns empty string (no section at all -- avoids cluttering the prompt)
   - If non-empty, builds a section like:
     ```
     --- INSPIRATIONS ---
     People this member admires and draws motivation from:
     - Steve Jobs: "Relentless product vision and reality distortion field"
     - Kobe Bryant (no context provided)
     ```
   - Appends guidance for Jarvis:
     ```
     When relevant, reference these inspirations naturally. For example, connect their current goal to something an inspiration is known for. If the member asks "what would [name] do?" answer based on what that person is widely known for -- their philosophy, approach, and public record. Don't invent private details. Keep inspiration references organic -- not every message needs one.
     ```

3. Insert the inspiration section into the sections array in buildSystemPrompt(), between the activity section and the conversation rules. This ensures Jarvis has the context before receiving behavioral instructions.

4. Update the buildProfileSection type signature to also accept the inspirations array and include it in the profile section. Actually NO -- keep inspirations as their own dedicated section (buildInspirationSection) for clarity since they are conceptually separate from the profile.

Key design point: The "what would [inspiration] do?" capability is NOT a separate command or special handler. Jarvis already has the inspiration names and context in its system prompt. When a member asks "what would Kobe do about this situation?" Jarvis sees the inspiration in its context and responds naturally. The guidance text tells Jarvis how to handle these queries. This is pure prompt engineering.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Review that buildSystemPrompt includes inspirations and the prompt guidance is clear and natural.</manual>
  </verify>
  <done>
    - buildSystemPrompt() queries and includes member inspirations
    - Inspiration section only appears when member has inspirations (no empty section)
    - Guidance tells Jarvis to reference inspirations naturally and handle "what would X do?" queries
    - briefs.ts and nudge.ts automatically get inspiration context because they call buildSystemPrompt()
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inspirations to protected data in memory.ts</name>
  <files>src/modules/ai-assistant/memory.ts</files>
  <action>
Update the memory system so inspirations are part of the protected member context that is never trimmed:

1. In assembleContext(), update the db.member.findUniqueOrThrow include to add:
   ```
   inspirations: {
     orderBy: { createdAt: 'asc' },
     select: { name: true, context: true },
   },
   ```

2. In buildMemberContext(), update the type signature to include:
   ```
   inspirations: Array<{ name: string; context: string | null }>;
   ```

3. In the body of buildMemberContext(), after the voice sessions block, add an inspirations block:
   - If inspirations array is non-empty:
     ```
     lines.push('\nInspirations:');
     for (const insp of member.inspirations) {
       if (insp.context) {
         lines.push(`  - ${insp.name}: ${insp.context}`);
       } else {
         lines.push(`  - ${insp.name}`);
       }
     }
     ```

4. Update the existing comment at the top of memory.ts where it says "Inspirations (when added in Phase 8)" to remove the "(when added in Phase 8)" qualifier since they're now implemented.

This ensures inspirations are PROTECTED data -- included verbatim in memberContext, never trimmed by the token budget loop (memberContext tokens are counted but never removed, per the existing trimming priority logic).
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify inspirations appear in buildMemberContext output and the Phase 8 comment is updated.</manual>
  </verify>
  <done>
    - assembleContext() includes inspirations in the member query
    - buildMemberContext() includes inspirations in the protected data string
    - Inspirations are never trimmed by token budget (protected data pattern)
    - Memory.ts header comment updated to reflect Phase 8 implementation
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- personality.ts buildSystemPrompt includes inspiration section with guidance text
- memory.ts buildMemberContext includes inspirations as protected data
- briefs.ts calls buildSystemPrompt -> automatically has inspirations (no changes needed)
- nudge.ts calls buildSystemPrompt -> automatically has inspirations (no changes needed)
- chat.ts builds messages from assembleContext -> memberContext has inspirations (no changes needed)
</verification>

<success_criteria>
- Jarvis's system prompt includes member's inspirations when they have any set
- "What would [inspiration] do?" is handled naturally through enriched system prompt (not a separate code path)
- Morning briefs and nudges have inspiration context available via buildSystemPrompt()
- Inspirations are in protected memory tier -- never compressed or trimmed
- No changes needed to briefs.ts, nudge.ts, or chat.ts -- they inherit the enrichment through existing function calls
</success_criteria>

<output>
After completion, create `.planning/phases/08-inspiration-system/08-02-SUMMARY.md`
</output>
