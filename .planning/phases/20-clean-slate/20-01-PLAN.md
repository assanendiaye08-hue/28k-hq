---
phase: 20-clean-slate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/src/modules/ai-assistant/index.ts
  - apps/bot/src/deploy-commands.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-04, CLEAN-03]

must_haves:
  truths:
    - "Bot starts and runs without the timer module -- no timer commands exist"
    - "Bot starts and runs without the auto-feed module -- no feed commands or scheduled fetches remain"
    - "Only /goals, /reminders, /leaderboard, and /announce-update slash commands are registered with Discord"
    - "AI assistant DM handler no longer references timer functions -- natural language timer requests fall through to regular chat"
  artifacts:
    - path: "apps/bot/src/deploy-commands.ts"
      provides: "Stripped slash command registration"
      contains: "buildGoalsCommand|buildRemindCommand|buildRemindersCommand|buildLeaderboardCommand|buildAnnounceUpdateCommand"
    - path: "apps/bot/src/modules/ai-assistant/index.ts"
      provides: "AI assistant without timer dependency"
  key_links:
    - from: "apps/bot/src/modules/ai-assistant/index.ts"
      to: "apps/bot/src/modules/ai-assistant/chat.ts"
      via: "handleChat for all DM messages (no timer intercept)"
      pattern: "handleChat"
---

<objective>
Remove legacy bot modules (timer, auto-feed) and strip slash commands down to the four that remain.

Purpose: The bot timer is replaced by the desktop app timer (shipped in v2.0). Auto-feed is legacy content curation that adds noise. Most slash commands are being replaced by conversational Jarvis in Phase 21. Cleaning these out reduces codebase surface, eliminates dead code paths, and prepares for the DM-first interaction model.

Output: Bot codebase with timer and auto-feed modules deleted, AI assistant cleaned of timer imports, deploy-commands.ts registering only 4 commands.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/bot/src/modules/timer/index.ts
@apps/bot/src/modules/auto-feed/index.ts
@apps/bot/src/modules/ai-assistant/index.ts
@apps/bot/src/deploy-commands.ts
@apps/bot/src/core/module-loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete timer and auto-feed modules, clean AI assistant timer references</name>
  <files>
    apps/bot/src/modules/timer/ (delete entire directory)
    apps/bot/src/modules/auto-feed/ (delete entire directory)
    apps/bot/src/modules/ai-assistant/index.ts
  </files>
  <action>
1. Delete the entire `apps/bot/src/modules/timer/` directory (7 files: buttons.ts, commands.ts, embeds.ts, engine.ts, index.ts, natural-language.ts, session.ts). The module-loader auto-discovers modules by directory -- removing the directory removes the module from the bot.

2. Delete the entire `apps/bot/src/modules/auto-feed/` directory (6 files: constants.ts, feedback.ts, filter.ts, index.ts, poster.ts, sources.ts). Same auto-discovery removal.

3. Edit `apps/bot/src/modules/ai-assistant/index.ts` to remove ALL timer-related code:
   - Remove these imports (lines 32-38):
     ```
     import { isTimerRequest, parseTimerRequest, isTimerStopRequest, isTimerPauseRequest, isTimerResumeRequest } from '../timer/natural-language.js';
     import { getActiveTimer, stopTimer, pauseTimer, resumeTimer, scheduleTransition } from '../timer/engine.js';
     import { startTimerForMember } from '../timer/index.js';
     import { persistTimerSession, deleteActiveTimerRecord, updateTimerRecord } from '../timer/session.js';
     import { buildTimerEmbed, buildTimerCompletedEmbed } from '../timer/embeds.js';
     import { buildWorkButtons, buildBreakButtons, buildPausedButtons } from '../timer/buttons.js';
     import { TIMER_DEFAULTS } from '@28k/shared';
     ```
   - Remove the entire `isTimerStopRequest` block (lines 98-127) that handles "stop timer" natural language
   - Remove the entire `isTimerPauseRequest` block (lines 129-168) that handles "pause timer" natural language
   - Remove the entire `isTimerResumeRequest` block (lines 170-219) that handles "resume timer" natural language
   - Remove the entire `isTimerRequest` block (lines 222-258) that handles "start timer" natural language
   - After these removals, the DM message handler flow should go: skip bots -> check DM/private channel -> skip setup users -> resolve member -> check reminder intent -> check decomposition intent -> show typing -> handleChat. Timer intent detection is completely gone.

4. Also in `ai-assistant/index.ts`, update the module doc comment at the top to remove mentions of "Natural language timer starts" since that feature is gone.

5. Check for `dist/` artifacts: delete `apps/bot/dist/modules/timer/` and `apps/bot/dist/modules/auto-feed/` directories if they exist (compiled JS from previous builds).
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && ! test -d apps/bot/src/modules/timer && ! test -d apps/bot/src/modules/auto-feed && ! grep -q "from.*timer" apps/bot/src/modules/ai-assistant/index.ts && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify ai-assistant/index.ts has no timer imports and the DM handler flows directly from reminder/decomposition checks to regular chat</manual>
  </verify>
  <done>Timer module directory deleted, auto-feed module directory deleted, AI assistant has zero references to timer module code, compiled dist directories cleaned</done>
</task>

<task type="auto">
  <name>Task 2: Strip deploy-commands.ts to four remaining slash commands</name>
  <files>
    apps/bot/src/deploy-commands.ts
  </files>
  <action>
Rewrite `apps/bot/src/deploy-commands.ts` to register ONLY these 4 commands:
- /goals (from `./modules/goals/commands.js` -- buildGoalsCommand)
- /reminders (from `./modules/reminders/commands.js` -- buildRemindersCommand)
- /leaderboard (from `./modules/leaderboard/commands.js` -- buildLeaderboardCommand)
- /announce-update (from `./modules/announce-update/commands.js` -- buildAnnounceUpdateCommand)

Remove ALL other command imports and registrations:
- Remove: buildCheckinCommand, buildSetgoalCommand, buildProgressCommand, buildCompletegoalCommand (checkin/goals)
- Remove: buildSettingsCommand (scheduler)
- Remove: buildSeasonCommand (season)
- Remove: buildAskCommand, buildWipeHistoryCommand, buildAccountabilityCommand (ai-assistant) -- NOTE: /ask is being removed as a slash command, but the AI assistant module still handles DMs. Users will just DM Jarvis directly instead of using /ask.
- Remove: buildLockinCommand, buildScheduleSessionCommand, buildEndsessionCommand, buildInviteSessionCommand (sessions)
- Remove: buildMydataCommand, buildDeletedataCommand (data-privacy)
- Remove: buildNotificationsCommand (notification-router)
- Remove: buildCostCommand, buildAdminSetModelCommand (ai-admin)
- Remove: buildInspirationCommand (inspiration)
- Remove: buildTimerCommand (timer -- module is deleted)
- Remove: buildRemindCommand (keep buildRemindersCommand only -- /reminders stays, /remind goes)
- Remove: /setup, /profile, /link, /verify, /unlink (Phase 1 identity commands built inline)

The resulting file should be clean and short -- just the 4 command imports and the deploy function.

Update the doc comment at the top to reflect the v3.0 command set.

IMPORTANT: Do NOT remove the modules themselves -- only their slash command registrations. The modules (checkin, sessions, etc.) still function through DMs, events, and cron jobs. The slash commands are just being stripped as entry points because Phase 21 replaces them with conversational Jarvis.

After editing, run `npx tsc --noEmit -p apps/bot/tsconfig.json` to verify no TypeScript errors from broken imports.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && grep -c "build.*Command\b" apps/bot/src/deploy-commands.ts | grep -q "4" && ! grep -q "buildTimerCommand\|buildCheckinCommand\|buildSetgoalCommand\|buildAskCommand\|buildLockinCommand\|buildInspirationCommand" apps/bot/src/deploy-commands.ts && echo "PASS" || echo "FAIL"</automated>
    <manual>Count command registrations -- should be exactly 4 (/goals, /reminders, /leaderboard, /announce-update)</manual>
  </verify>
  <done>deploy-commands.ts registers exactly 4 slash commands, all removed command imports are gone, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Build verification and deploy commands</name>
  <files>
    apps/bot/src/deploy-commands.ts
  </files>
  <action>
1. Run TypeScript compilation check across the bot package to verify no broken imports after module deletions:
   ```
   cd apps/bot && npx tsc --noEmit
   ```
   If there are compilation errors from other modules importing from the deleted timer module, fix them:
   - Any module importing from `../timer/` needs those imports removed
   - The `apps/bot/src/core/events.ts` may have timer event type references -- check and clean if needed

2. Run the deploy-commands script in guild mode to immediately update Discord:
   ```
   cd apps/bot && npx tsx src/deploy-commands.ts
   ```
   This will replace ALL currently registered commands with just the 4 remaining ones. Discord will remove the old commands from the command palette.

3. Verify the bot can still build successfully:
   ```
   cd apps/bot && npm run build
   ```
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler/apps/bot" && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Bot compiles without errors, deploy-commands script runs successfully</manual>
  </verify>
  <done>Bot compiles cleanly with no timer/auto-feed references, slash commands deployed to Discord showing only 4 commands, build succeeds</done>
</task>

</tasks>

<verification>
1. `ls apps/bot/src/modules/` shows no `timer/` or `auto-feed/` directories
2. `grep -r "from.*timer" apps/bot/src/modules/` returns no results (except reminders which has its own timer-unrelated code)
3. `grep -r "auto-feed" apps/bot/src/modules/` returns no results
4. `grep -c "buildGoalsCommand\|buildRemindersCommand\|buildLeaderboardCommand\|buildAnnounceUpdateCommand" apps/bot/src/deploy-commands.ts` returns 4
5. `cd apps/bot && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Bot timer module completely removed (directory, imports, references)
- Auto-feed module completely removed (directory, imports, references)
- AI assistant DM handler no longer intercepts timer intent -- all messages flow to chat or existing intent handlers (reminders, decomposition)
- Only /goals, /reminders, /leaderboard, /announce-update registered as slash commands
- TypeScript compiles without errors
- Slash commands deployed to Discord
</success_criteria>

<output>
After completion, create `.planning/phases/20-clean-slate/20-01-SUMMARY.md`
</output>
