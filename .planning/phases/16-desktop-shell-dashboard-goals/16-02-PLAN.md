---
phase: 16-desktop-shell-dashboard-goals
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/desktop/src/stores/dashboard-store.ts
  - apps/desktop/src/pages/DashboardPage.tsx
  - apps/desktop/src/components/layout/AppShell.tsx
  - apps/desktop/src/components/layout/Sidebar.tsx
  - apps/desktop/src/components/dashboard/PriorityList.tsx
  - apps/desktop/src/components/dashboard/WeeklyGoals.tsx
  - apps/desktop/src/components/dashboard/StreakBadge.tsx
  - apps/desktop/src/components/dashboard/RankProgress.tsx
  - apps/desktop/src/components/dashboard/DailyQuote.tsx
  - apps/desktop/src/components/common/Card.tsx
  - apps/desktop/src/components/common/ProgressBar.tsx
  - apps/desktop/src/App.tsx
autonomous: true
requirements: [APP-04, DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Authenticated user sees dashboard as the landing page after login"
    - "Dashboard shows today's active goals as a priority list"
    - "Dashboard shows this week's goals with progress"
    - "Dashboard shows current streak with fire visual when streak > 0"
    - "Dashboard shows current rank name and XP progress bar to next rank"
    - "Dashboard shows a daily rotating operator quote"
    - "Sidebar navigation allows switching between Dashboard and Goals views"
    - "All dashboard data loads from the API /dashboard endpoint"
  artifacts:
    - path: "apps/desktop/src/stores/dashboard-store.ts"
      provides: "Zustand store for dashboard data fetching and state"
      exports: ["useDashboardStore"]
    - path: "apps/desktop/src/pages/DashboardPage.tsx"
      provides: "Dashboard page composing all dashboard cards"
      min_lines: 30
    - path: "apps/desktop/src/components/layout/AppShell.tsx"
      provides: "Main layout with sidebar + content area"
      min_lines: 15
    - path: "apps/desktop/src/components/dashboard/PriorityList.tsx"
      provides: "Today's priorities card rendering goal list"
      min_lines: 15
    - path: "apps/desktop/src/components/dashboard/StreakBadge.tsx"
      provides: "Streak display with fire visual for active streaks"
      min_lines: 10
    - path: "apps/desktop/src/components/dashboard/RankProgress.tsx"
      provides: "Rank name, XP count, progress bar to next rank"
      min_lines: 15
    - path: "apps/desktop/src/components/dashboard/DailyQuote.tsx"
      provides: "Daily quote card with text and author"
      min_lines: 10
  key_links:
    - from: "apps/desktop/src/stores/dashboard-store.ts"
      to: "GET /dashboard"
      via: "apiFetch from client.ts"
      pattern: "apiFetch.*dashboard"
    - from: "apps/desktop/src/pages/DashboardPage.tsx"
      to: "apps/desktop/src/stores/dashboard-store.ts"
      via: "useDashboardStore hook"
      pattern: "useDashboardStore"
    - from: "apps/desktop/src/components/dashboard/RankProgress.tsx"
      to: "dashboard-store data.member"
      via: "props from DashboardPage"
      pattern: "totalXp|nextRank"
---

<objective>
Build the dashboard page with all five data cards (priorities, weekly goals, streak, rank+XP, quote) and the app shell layout with sidebar navigation.

Purpose: The dashboard is the landing page -- the first thing users see after login. It surfaces today's priorities, progress, and motivation at a glance, making the app immediately useful.
Output: A fully styled dashboard consuming the /dashboard API endpoint with sidebar navigation between Dashboard and Goals views.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-desktop-shell-dashboard-goals/16-RESEARCH.md
@.planning/phases/16-desktop-shell-dashboard-goals/16-01-SUMMARY.md
@apps/api/src/routes/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell layout, sidebar, reusable components, and dashboard store</name>
  <files>
    apps/desktop/src/components/layout/AppShell.tsx
    apps/desktop/src/components/layout/Sidebar.tsx
    apps/desktop/src/components/common/Card.tsx
    apps/desktop/src/components/common/ProgressBar.tsx
    apps/desktop/src/stores/dashboard-store.ts
    apps/desktop/src/App.tsx
  </files>
  <action>
    Build the app shell layout and supporting infrastructure for the dashboard.

    **src/components/layout/Sidebar.tsx**: Left sidebar navigation panel (width ~220px):
    - Dark surface-1 background with border-r border-border
    - At top: small gold 28K HQ logo text or icon placeholder
    - Nav links: Dashboard (home icon) and Goals (target icon). Use simple SVG icons or unicode symbols. Active link highlighted with brand gold background/text, inactive links in text-secondary with hover to text-primary.
    - Use NavLink from react-router for active state detection
    - At bottom: user display name from useAuthStore (small text), and a logout button (text-tertiary, hover text-error)
    - On logout: call logoutSession() from api/auth.ts, then authStore.logout(), navigate to /login

    **src/components/layout/AppShell.tsx**: Main layout wrapper:
    - Flex row: Sidebar on left, content area on right (flex-1)
    - Content area: bg-surface-base, overflow-y-auto, padding for content breathing room
    - Takes children prop for the page content

    **src/components/common/Card.tsx**: Reusable dark card component:
    - Props: title (optional string), className (optional), children
    - Style: bg-surface-1 border border-border rounded-lg p-5
    - If title: render h3 in text-brand font-semibold mb-3
    - Used by all dashboard and goals components

    **src/components/common/ProgressBar.tsx**: Reusable horizontal progress bar:
    - Props: value (0-100), color (optional, defaults to brand gold), size (sm/md, defaults md)
    - Outer: bg-surface-2 rounded-full, height based on size (sm=6px, md=8px)
    - Inner: rounded-full with percentage width, color applied as bg class
    - Clamp value to 0-100

    **src/stores/dashboard-store.ts**: Following 16-RESEARCH.md Pattern 5:
    - Interface DashboardData matching the API /dashboard response exactly:
      - member: { displayName, totalXp, currentStreak, longestStreak, rank, rankColor, nextRank: { name, xpRequired, xpRemaining } | null }
      - goals: { today: Goal[], weekly: Goal[] } where Goal has id, title, type, status, currentValue, targetValue, timeframe, deadline, children
      - timer: object | null (not used this phase but include for type completeness)
      - todayCheckins: number
      - quote: { text: string, author: string }
    - State: data (DashboardData | null), isLoading, error (string | null)
    - Actions: fetchDashboard() -- calls apiFetch<DashboardData>('/dashboard'), sets data/error/loading

    **src/App.tsx (update)**: Wrap authenticated routes in AppShell layout:
    - / -> DashboardPage (inside AppShell)
    - /goals -> GoalsPage placeholder (inside AppShell) -- just render "Goals coming soon" text
    - /login -> LoginPage (no AppShell)
    - Keep all existing auth logic from Plan 01
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord\ Hustler/apps/desktop && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Run pnpm tauri dev, login, verify sidebar appears on left with Dashboard and Goals links, content area on right</manual>
  </verify>
  <done>App shell renders with sidebar navigation, Card and ProgressBar components exist, dashboard store is wired to API /dashboard endpoint</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page with all five data cards (priorities, weekly goals, streak, rank, quote)</name>
  <files>
    apps/desktop/src/pages/DashboardPage.tsx
    apps/desktop/src/components/dashboard/PriorityList.tsx
    apps/desktop/src/components/dashboard/WeeklyGoals.tsx
    apps/desktop/src/components/dashboard/StreakBadge.tsx
    apps/desktop/src/components/dashboard/RankProgress.tsx
    apps/desktop/src/components/dashboard/DailyQuote.tsx
  </files>
  <action>
    Build all five dashboard cards and compose them into the DashboardPage.

    **src/components/dashboard/PriorityList.tsx**: Today's priorities card:
    - Props: goals (Goal array from dashboard.goals.today)
    - Wrapped in Card with title "Today's Priorities"
    - Render each goal as a row: status indicator (green checkmark if COMPLETED, gold circle if ACTIVE), title text, and for MEASURABLE goals show "current/target" with a small ProgressBar
    - Empty state: "No active priorities" in text-tertiary
    - Goals should show their timeframe badge (e.g., "Weekly" in a small pill)

    **src/components/dashboard/WeeklyGoals.tsx**: This week's goals card:
    - Props: goals (Goal array from dashboard.goals.weekly), className optional
    - Wrapped in Card with title "This Week"
    - Similar to PriorityList but focused on weekly goals
    - Show progress fraction at top: "3/5 goals complete" in text-secondary
    - Render each goal with completion status and progress
    - ProgressBar showing overall weekly completion percentage

    **src/components/dashboard/StreakBadge.tsx**: Streak display:
    - Props: currentStreak (number), longestStreak (number)
    - Wrapped in Card with title "Streak"
    - Large streak number in text-brand font-bold text-3xl
    - If streak > 0: show fire emoji or fire-colored icon next to the number, text "days" label
    - If streak === 0: show "No active streak" in text-tertiary, muted styling
    - Below: "Longest: {longestStreak} days" in text-secondary text-sm

    **src/components/dashboard/RankProgress.tsx**: Rank and XP display:
    - Props: rank (string), rankColor (number -- hex color from API), totalXp (number), nextRank ({ name, xpRequired, xpRemaining } | null)
    - Wrapped in Card with title "Rank"
    - Show rank name in large text, colored with the rankColor (convert from hex int to CSS color: `#${rankColor.toString(16).padStart(6, '0')}`)
    - Show totalXp as "1,234 XP" formatted with commas
    - If nextRank exists: ProgressBar showing progress toward next rank (percentage = (xpRequired - xpRemaining) / xpRequired * 100), label "{xpRemaining} XP to {nextRank.name}" in text-secondary
    - If nextRank is null (max rank): show "Max rank achieved" in text-brand

    **src/components/dashboard/DailyQuote.tsx**: Daily operator quote:
    - Props: quote ({ text: string, author: string })
    - Wrapped in Card (no title -- the quote speaks for itself)
    - Quote text in italic, text-text-primary, text-lg, with decorative gold left border (border-l-2 border-brand pl-4)
    - Author attribution below: "-- {author}" in text-text-secondary text-sm, right-aligned

    **src/pages/DashboardPage.tsx**: Compose all cards:
    - On mount (useEffect): call dashboardStore.fetchDashboard()
    - While loading: show LoadingSpinner centered
    - On error: show error message with retry button
    - Layout: CSS Grid with responsive layout
      - Top row: greeting "Good {morning/afternoon/evening}, {displayName}" in text-2xl
      - Main grid (2 columns on wider screens, 1 on narrow):
        - Left column: PriorityList (full width), WeeklyGoals below it
        - Right column: StreakBadge, RankProgress, DailyQuote stacked
      - Use Tailwind grid: `grid grid-cols-1 lg:grid-cols-3 gap-4` where priorities take lg:col-span-2
    - Pass data from useDashboardStore to each card component
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord\ Hustler/apps/desktop && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Run pnpm tauri dev, login with a real account that has goals/streak data, verify all 5 dashboard cards render with real data from the API</manual>
  </verify>
  <done>Dashboard page renders all five cards (priorities, weekly goals, streak, rank+XP, quote) with data from the /dashboard API endpoint. Empty states handled gracefully. Dark+gold theme consistently applied.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in apps/desktop
2. App launches, authenticated user sees dashboard with sidebar
3. Dashboard fetches from /dashboard endpoint and renders 5 cards
4. Sidebar navigation switches between Dashboard and Goals (placeholder)
5. Streak shows fire visual when > 0
6. Rank shows colored name and XP progress bar
7. Quote displays with gold left border
8. Empty states display gracefully for users with no goals/streak
</verification>

<success_criteria>
- Dashboard is the landing page after login
- All 5 data cards render with real API data
- App shell with sidebar navigation works between Dashboard and Goals
- Dark+gold theme consistent across all components
- Loading and error states handled
</success_criteria>

<output>
After completion, create `.planning/phases/16-desktop-shell-dashboard-goals/16-02-SUMMARY.md`
</output>
