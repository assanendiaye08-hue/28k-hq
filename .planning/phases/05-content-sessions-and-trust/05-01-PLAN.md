---
phase: 05-content-sessions-and-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/modules/xp/constants.ts
  - src/modules/server-setup/channels.ts
  - src/shared/constants.ts
  - src/modules/resources/index.ts
  - src/modules/resources/handler.ts
  - src/modules/resources/tagger.ts
  - src/modules/resources/constants.ts
  - src/deploy-commands.ts
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "A post in #tech-resources, #business-resources, or #growth-resources triggers bot reaction and auto-thread creation"
    - "AI auto-tags each resource post with topic and interest tags asynchronously"
    - "Resource sharing awards 15 XP with a 4-hour cooldown per member"
    - "Discussion threads keep resource channels clean and organized"
  artifacts:
    - path: "src/modules/resources/handler.ts"
      provides: "Resource post detection, thread creation, XP award"
      min_lines: 60
    - path: "src/modules/resources/tagger.ts"
      provides: "AI-powered resource auto-tagging via OpenRouter"
      min_lines: 40
    - path: "src/modules/resources/index.ts"
      provides: "Module registration with messageCreate listener"
      exports: ["default"]
    - path: "src/modules/resources/constants.ts"
      provides: "Resource channel names, XP amount, cooldown, emoji"
      min_lines: 10
  key_links:
    - from: "src/modules/resources/handler.ts"
      to: "src/modules/xp/engine.ts"
      via: "awardXP() call for RESOURCE_SHARE source"
      pattern: "awardXP.*RESOURCE_SHARE"
    - from: "src/modules/resources/handler.ts"
      to: "message.startThread()"
      via: "Discord.js thread creation API"
      pattern: "startThread"
    - from: "src/modules/resources/tagger.ts"
      to: "@openrouter/sdk"
      via: "AI structured output for tag extraction"
      pattern: "OpenRouter|chat\\.send"
---

<objective>
Build the resources module -- resource sharing channels with AI-powered auto-tagging, discussion threads, and XP rewards.

Purpose: Members can share resources (articles, tools, tutorials) in interest-organized channels. Bot auto-creates discussion threads to keep channels clean, AI-tags posts for searchability, and awards XP to incentivize sharing.
Output: Working resources module auto-discovered by module loader, 3 resource channels in server setup, RESOURCE_SHARE XP source.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-sessions-and-trust/05-CONTEXT.md
@.planning/phases/05-content-sessions-and-trust/05-RESEARCH.md
@prisma/schema.prisma
@src/modules/wins-lessons/handler.ts
@src/modules/wins-lessons/constants.ts
@src/modules/wins-lessons/index.ts
@src/modules/profile/ai-tags.ts
@src/modules/xp/engine.ts
@src/modules/xp/constants.ts
@src/modules/server-setup/channels.ts
@src/shared/constants.ts
@src/deploy-commands.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema extension, XP constants, and server-setup channel additions</name>
  <files>
    prisma/schema.prisma
    src/modules/xp/constants.ts
    src/modules/server-setup/channels.ts
  </files>
  <action>
    1. In `prisma/schema.prisma`, add `RESOURCE_SHARE` to the existing `XPSource` enum (after LESSON_POST).

    2. In `src/modules/xp/constants.ts`, add resource sharing XP config to the XP_AWARDS object:
       - `resourceShare: 15` -- 15 XP per resource post (lower than wins/lessons since it's lower effort)
       - `resourceShareCooldownMs: 4 * 60 * 60 * 1000` -- 4-hour cooldown per member (prevents spam)

    3. In `src/modules/server-setup/channels.ts`, update the CATEGORIES array to replace the single `resources` channel under the RESOURCES category with three channels:
       - `{ name: 'tech-resources', type: ChannelType.GuildText }`
       - `{ name: 'business-resources', type: ChannelType.GuildText }`
       - `{ name: 'growth-resources', type: ChannelType.GuildText }`

    Run `npx prisma validate` and `npx prisma generate` to verify schema. Run `npx tsc --noEmit` to verify TypeScript compilation.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx prisma validate && npx prisma generate && npx tsc --noEmit</automated>
  </verify>
  <done>XPSource enum includes RESOURCE_SHARE, XP_AWARDS has resourceShare config, RESOURCES category has 3 channels instead of 1, everything compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Resources module with handler, AI tagger, and module registration</name>
  <files>
    src/modules/resources/constants.ts
    src/modules/resources/tagger.ts
    src/modules/resources/handler.ts
    src/modules/resources/index.ts
    src/deploy-commands.ts
  </files>
  <action>
    1. Create `src/modules/resources/constants.ts`:
       - `RESOURCE_CHANNELS = ['tech-resources', 'business-resources', 'growth-resources']`
       - `RESOURCE_EMOJI = '\uD83D\uDD17'` (link emoji for bot reaction)
       - `THREAD_WELCOME = 'Thread created for discussion. Share your thoughts on this resource!'`

    2. Create `src/modules/resources/tagger.ts`:
       - Follow the exact pattern from `src/modules/profile/ai-tags.ts` (OpenRouter SDK with structured JSON output)
       - Export `extractResourceTags(messageContent: string): Promise<{ topic: string; tags: string[] }>`
       - Use DeepSeek V3.2 model via OpenRouter
       - System prompt: extract a short topic title (5-10 words) and 2-4 interest tags from the resource post text
       - Use `responseFormat` with `jsonSchema` (camelCase API per [01-04] decision)
       - Fallback: return `{ topic: 'Resource', tags: [] }` if AI fails
       - This is fire-and-forget (called async, non-blocking on the main handler flow)
       - Do NOT fetch external URLs from the post -- only extract from message text

    3. Create `src/modules/resources/handler.ts`:
       - Export `handleResourcePost(message: Message, db: ExtendedPrismaClient, events: IEventBus): Promise<void>`
       - Follow the wins-lessons/handler.ts pattern exactly:
         a. Skip if message.author.bot or !message.guild
         b. Check if channel name is in RESOURCE_CHANNELS array
         c. Resolve member via `db.discordAccount.findUnique({ where: { discordId: message.author.id } })`
         d. Skip if not a registered member
         e. React with RESOURCE_EMOJI immediately
         f. Create discussion thread: `message.startThread({ name: threadName, autoArchiveDuration: ThreadAutoArchiveDuration.OneDay, reason: 'Auto-thread for resource discussion' })`
            - Thread name: `'Discuss: ' + truncated first line of message (max 80 chars, add '...' if truncated)`
            - Send THREAD_WELCOME message in the thread
         g. Award XP: check cooldown (same per-member cooldown map pattern as wins-lessons), award 15 XP with source RESOURCE_SHARE if not on cooldown
         h. Fire-and-forget: call `extractResourceTags(message.content)` then update the thread name to `'Discuss: ' + topic` if AI returns a meaningful topic. Wrap in try/catch -- failure is silent.

    4. Create `src/modules/resources/index.ts`:
       - Standard module pattern (export default with name: 'resources' and register function)
       - Listen to `messageCreate` event on `ctx.client`
       - Call handleResourcePost in the handler
       - Log module registered

    5. Update `src/deploy-commands.ts`:
       - No new slash commands needed for resources (it's passive -- responds to messages, not commands)
       - Only update the comment header to mention Phase 5

    Run `npx tsc --noEmit` to verify.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit</automated>
  </verify>
  <done>Resources module exists with handler (thread creation + XP), AI tagger (OpenRouter structured output), constants, and module index. Module will be auto-discovered by loader. Posts in resource channels trigger reaction + thread + optional XP.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx prisma validate` succeeds
- Resources module directory exists with 4 files (constants, tagger, handler, index)
- XPSource enum includes RESOURCE_SHARE
- RESOURCES category in server-setup has 3 channels
</verification>

<success_criteria>
- Resource channel posts trigger: bot reaction, auto-thread creation, XP award with cooldown, async AI tagging
- Thread names are truncated to 80 chars from message first line (or AI-extracted topic)
- XP awards use RESOURCE_SHARE source at 15 XP with 4-hour cooldown
- Module auto-discovered by existing module loader
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-sessions-and-trust/05-01-SUMMARY.md`
</output>
