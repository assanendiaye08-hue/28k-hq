---
phase: 05-content-sessions-and-trust
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/modules/data-privacy/index.ts
  - src/modules/data-privacy/commands.ts
  - src/modules/data-privacy/exporter.ts
  - src/modules/data-privacy/deleter.ts
  - src/modules/data-privacy/constants.ts
  - src/db/encryption.ts
  - src/deploy-commands.ts
autonomous: true
requirements: [TRUST-01, TRUST-02, TRUST-03]

must_haves:
  truths:
    - "Member can run /mydata and receive a JSON file via DM containing ALL their stored data"
    - "Member can run /deletedata, confirm, and have all their data permanently wiped"
    - "After /deletedata, member's roles are stripped and private channel deleted"
    - "Private conversations and personal text fields are encrypted at rest -- DB access shows only ciphertext"
    - "DM delivery failure for /mydata falls back to private space channel"
  artifacts:
    - path: "src/modules/data-privacy/exporter.ts"
      provides: "Full data export as JSON buffer with all member tables"
      min_lines: 60
    - path: "src/modules/data-privacy/deleter.ts"
      provides: "Hard delete with cascade, Discord role/channel cleanup"
      min_lines: 50
    - path: "src/modules/data-privacy/commands.ts"
      provides: "/mydata and /deletedata command definitions and handlers"
      min_lines: 80
    - path: "src/modules/data-privacy/index.ts"
      provides: "Module registration with command wiring"
      exports: ["default"]
    - path: "src/db/encryption.ts"
      provides: "Updated ENCRYPTED_FIELDS covering all personal text fields including Phase 5"
  key_links:
    - from: "src/modules/data-privacy/exporter.ts"
      to: "@prisma/client"
      via: "Prisma include with all relations for complete data export"
      pattern: "include.*accounts|checkIns|goals|xpTransactions"
    - from: "src/modules/data-privacy/deleter.ts"
      to: "@prisma/client"
      via: "db.member.delete cascade"
      pattern: "member\\.delete"
    - from: "src/modules/data-privacy/commands.ts"
      to: "discord.js AttachmentBuilder"
      via: "JSON file sent as DM attachment"
      pattern: "AttachmentBuilder"
---

<objective>
Build the data-privacy module -- /mydata full data export, /deletedata hard deletion, and encryption verification for owner-blind privacy.

Purpose: Members have full transparency into what the bot stores about them and can permanently delete everything. Private data remains encrypted at rest so even server admins or DB access cannot read personal information.
Output: Working data-privacy module with /mydata and /deletedata commands, verified encryption coverage across all personal text fields.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-sessions-and-trust/05-CONTEXT.md
@.planning/phases/05-content-sessions-and-trust/05-RESEARCH.md
@.planning/phases/05-content-sessions-and-trust/05-01-SUMMARY.md
@.planning/phases/05-content-sessions-and-trust/05-02-SUMMARY.md
@prisma/schema.prisma
@src/db/encryption.ts
@src/shared/crypto.ts
@src/modules/ai-assistant/commands.ts
@src/shared/delivery.ts
@src/deploy-commands.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encryption audit, data exporter, and data deleter</name>
  <files>
    src/db/encryption.ts
    src/modules/data-privacy/constants.ts
    src/modules/data-privacy/exporter.ts
    src/modules/data-privacy/deleter.ts
  </files>
  <action>
    1. Audit `src/db/encryption.ts` ENCRYPTED_FIELDS map and verify ALL personal text fields are covered:
       - Already encrypted: MemberProfile.rawAnswers, CheckIn.content, Goal.description, ConversationMessage.content, ConversationSummary.summary
       - Review Phase 5 models: LockInSession.title contains what the member is working on -- this is personal context. Add it to ENCRYPTED_FIELDS: `LockInSession: ['title']`
       - The encryption extension extracts memberId from the record for key derivation. LockInSession uses `creatorMemberId` not `memberId`. Update `extractMemberId` to also check for `creatorMemberId` field in the data/where/create/update args. This is a small targeted change -- add checks for `creatorMemberId` alongside existing `memberId` checks in the extractMemberId function.
       - Also update `extractMemberIdFromResult` to check `creatorMemberId` on result objects.

    2. Create `src/modules/data-privacy/constants.ts`:
       - `EXPORT_VERSION = 1` -- version number embedded in JSON export
       - `DELETE_CONFIRMATION_TIMEOUT_MS = 30 * 1000` -- 30 seconds to confirm deletion
       - `DELETE_CONFIRMATION_WORD = 'DELETE'` -- what user must type to confirm
       - `EXPORT_FILENAME_PREFIX = 'mydata'`

    3. Create `src/modules/data-privacy/exporter.ts`:
       - Export `exportMemberData(db: ExtendedPrismaClient, memberId: string): Promise<Buffer>`
       - Query the Member record with ALL relations included:
         ```
         db.member.findUniqueOrThrow({
           where: { id: memberId },
           include: {
             accounts: { select: { discordId: true, linkedAt: true } },
             profile: true,
             checkIns: { orderBy: { createdAt: 'asc' } },
             goals: { orderBy: { createdAt: 'asc' } },
             xpTransactions: { orderBy: { createdAt: 'asc' } },
             voiceSessions: { orderBy: { startedAt: 'asc' } },
             conversationMessages: { orderBy: { createdAt: 'asc' } },
             conversationSummary: true,
             schedule: true,
             seasonSnapshots: true,
             privateSpace: true,
           },
         })
         ```
       - Also query SessionParticipant records for this member (separate query since there's no direct Member relation):
         `db.sessionParticipant.findMany({ where: { memberId }, include: { session: true } })`
       - Build structured export object:
         ```
         {
           exportVersion: EXPORT_VERSION,
           exportedAt: new Date().toISOString(),
           member: { id, displayName, totalXp, currentStreak, longestStreak, lastCheckInAt, createdAt, updatedAt },
           accounts: [...],
           profile: { interests, currentFocus, goals, learningAreas, workStyle, publicFields, rawAnswers },
           checkIns: [...],
           goals: [...],
           xpHistory: [...],
           voiceSessions: [...],
           conversations: [...],
           conversationSummary: { ... } or null,
           schedule: { ... } or null,
           seasonSnapshots: [...],
           privateSpace: { type, channelId } or null,
           sessionParticipation: [...],  // Sessions they participated in
         }
         ```
       - Note: Encrypted fields (rawAnswers, content, description, summary) will be automatically decrypted by the encryption extension when read. The export shows plaintext -- this is correct because only the member themselves receives it.
       - Return `Buffer.from(JSON.stringify(exportData, null, 2), 'utf-8')`

    4. Create `src/modules/data-privacy/deleter.ts`:
       - Export `hardDeleteMember(db: ExtendedPrismaClient, client: Client, memberId: string, guild: Guild): Promise<void>`
       - Follow this exact order (Discord cleanup BEFORE database deletion):
         a. Fetch all linked Discord accounts: `db.discordAccount.findMany({ where: { memberId }, select: { discordId: true } })`
         b. Fetch private space: `db.privateSpace.findUnique({ where: { memberId } })`
         c. Delete private space channel if exists (try/catch -- channel may be gone):
            ```
            if (space?.channelId) {
              const ch = await client.channels.fetch(space.channelId).catch(() => null);
              if (ch) await ch.delete('Member data deletion').catch(() => {});
            }
            ```
         d. Clean up SessionParticipant records for this member (separate since no cascade): `db.sessionParticipant.deleteMany({ where: { memberId } })`
         e. Delete Member record: `db.member.delete({ where: { id: memberId } })` -- this cascades to ALL related tables (DiscordAccount, MemberProfile, PrivateSpace, CheckIn, Goal, XPTransaction, MemberSchedule, VoiceSession, ConversationMessage, ConversationSummary, SeasonSnapshot). Do NOT write individual deleteMany calls.
         f. Strip ALL bot-managed roles from all linked Discord accounts:
            ```
            for (const account of accounts) {
              try {
                const guildMember = await guild.members.fetch(account.discordId);
                // Remove all roles except @everyone (which can't be removed)
                const botRoles = guildMember.roles.cache.filter(r => r.name !== '@everyone');
                for (const [, role] of botRoles) {
                  await guildMember.roles.remove(role).catch(() => {});
                }
              } catch { /* Member may have left server */ }
            }
            ```

    Run `npx tsc --noEmit` to verify.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit</automated>
  </verify>
  <done>Encryption covers LockInSession.title with creatorMemberId extraction. Exporter queries all tables and builds complete JSON. Deleter does Discord cleanup first then cascade delete. All compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Slash commands, module registration, and deploy script update</name>
  <files>
    src/modules/data-privacy/commands.ts
    src/modules/data-privacy/index.ts
    src/deploy-commands.ts
  </files>
  <action>
    1. Create `src/modules/data-privacy/commands.ts`:
       - Export `buildMydataCommand()`: /mydata (no options)
         - Handler:
           a. Defer reply as ephemeral
           b. Resolve member from Discord account
           c. Call `exportMemberData(db, memberId)`
           d. Create `AttachmentBuilder` with the JSON buffer: `new AttachmentBuilder(jsonBuffer, { name: 'mydata-{displayName}-{timestamp}.json' })`
           e. Try to DM the user with the file attachment: `interaction.user.send({ content: 'Here is all the data stored about you.', files: [attachment] })`
           f. If DM fails (user has DMs closed), try delivering via private space channel using `deliverToPrivateSpace` with the file as an attachment
           g. If both fail, reply ephemerally: "I couldn't send you the file. Please enable DMs from server members or switch to a private channel space."
           h. On success, reply ephemerally: "Your data has been sent to your DMs. Check your messages!"

       - Export `buildDeletedataCommand()`: /deletedata (no options)
         - Handler:
           a. Reply ephemerally with a serious warning embed: "This will PERMANENTLY delete ALL your data -- profile, check-ins, goals, XP, conversations, everything. This cannot be undone. Type DELETE to confirm within 30 seconds."
           b. Await a follow-up message from the user in the same channel within DELETE_CONFIRMATION_TIMEOUT_MS
              - Use `interaction.channel.awaitMessages({ filter: m => m.author.id === interaction.user.id, max: 1, time: DELETE_CONFIRMATION_TIMEOUT_MS })`
              - If no response or response !== 'DELETE': reply ephemerally "Data deletion cancelled."
              - If response is 'DELETE':
                1. Reply "Deleting all your data..."
                2. Call `hardDeleteMember(db, client, memberId, guild)`
                3. Edit the reply: "All your data has been permanently deleted. Your roles have been removed. You can re-join anytime by running /setup."
                4. Clean up the confirmation message if possible

    2. Create `src/modules/data-privacy/index.ts`:
       - Standard module pattern (export default, name: 'data-privacy')
       - Register /mydata and /deletedata commands
       - Log module registered

    3. Update `src/deploy-commands.ts`:
       - Import buildMydataCommand, buildDeletedataCommand from data-privacy/commands.ts
       - Add /mydata and /deletedata to the commands array
       - Update comment header to include Phase 5 data privacy commands
       - Final count should be: 5 (Phase 1) + 6 (Phase 2) + 2 (Phase 3) + 3 (Phase 4) + 4 (Phase 5 sessions) + 2 (Phase 5 data privacy) = 22 commands total

    Run `npx tsc --noEmit` to verify.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit</automated>
  </verify>
  <done>/mydata exports complete JSON of all member data via DM (with private space fallback). /deletedata with DELETE confirmation hard-deletes everything with cascade + Discord cleanup. Data-privacy module auto-discovered. 22 total commands in deploy script. All compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Data-privacy module directory exists with 5 files (constants, exporter, deleter, commands, index)
- ENCRYPTED_FIELDS includes LockInSession.title with creatorMemberId extraction
- /mydata and /deletedata commands registered in deploy-commands.ts
- Encryption covers: rawAnswers, content (CheckIn), description (Goal), content (ConversationMessage), summary (ConversationSummary), title (LockInSession)
</verification>

<success_criteria>
- /mydata sends a complete JSON file with ALL member data (profile, check-ins, goals, XP, voice, conversations, schedule, snapshots, session participation)
- /mydata falls back to private space channel if DM delivery fails
- /deletedata requires typing DELETE within 30 seconds to confirm
- After /deletedata: all DB records gone (cascade), roles stripped, private channel deleted
- Encrypted fields include LockInSession.title -- DB access shows only ciphertext for all personal text
- No manual deleteMany calls -- single db.member.delete() cascades everything
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-sessions-and-trust/05-03-SUMMARY.md`
</output>
