---
phase: 24-desktop-app-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/pages/DashboardPage.tsx
  - apps/desktop/src/components/dashboard/TodayView.tsx
  - apps/desktop/src/components/dashboard/TimerWidget.tsx
  - apps/desktop/src/components/dashboard/SessionHistory.tsx
  - apps/desktop/src/stores/history-store.ts
  - apps/api/src/routes/timer.ts
autonomous: true
requirements: [APP-01, APP-02]

must_haves:
  truths:
    - "Member opens the app and sees today's priorities, active timer status, and streak in a single glanceable view"
    - "Member sees their current focus and timer countdown inline on the dashboard without navigating to /timer"
    - "Member can view past work sessions with focus labels and durations"
    - "Dashboard is glanceable in 2 seconds -- no visual noise from weekly goals, rank progress, or quotes"
  artifacts:
    - path: "apps/desktop/src/components/dashboard/TodayView.tsx"
      provides: "Focused cockpit component combining timer widget, today's goals, and streak"
      min_lines: 60
    - path: "apps/desktop/src/components/dashboard/TimerWidget.tsx"
      provides: "Inline timer status showing phase, countdown, and focus label"
      min_lines: 40
    - path: "apps/desktop/src/components/dashboard/SessionHistory.tsx"
      provides: "List of completed sessions with focus label and duration"
      min_lines: 40
    - path: "apps/desktop/src/stores/history-store.ts"
      provides: "Zustand store for fetching session history from API"
      exports: ["useHistoryStore"]
    - path: "apps/api/src/routes/timer.ts"
      provides: "Enhanced GET /history endpoint with goal title join"
      contains: "goal"
  key_links:
    - from: "apps/desktop/src/components/dashboard/TodayView.tsx"
      to: "apps/desktop/src/stores/dashboard-store.ts"
      via: "useDashboardStore hook"
      pattern: "useDashboardStore"
    - from: "apps/desktop/src/components/dashboard/TimerWidget.tsx"
      to: "apps/desktop/src/stores/timer-store.ts"
      via: "useTimerStore hook for live timer state"
      pattern: "useTimerStore"
    - from: "apps/desktop/src/components/dashboard/SessionHistory.tsx"
      to: "apps/desktop/src/stores/history-store.ts"
      via: "useHistoryStore for fetching /timer/history"
      pattern: "useHistoryStore"
    - from: "apps/desktop/src/stores/history-store.ts"
      to: "/timer/history"
      via: "apiFetch GET"
      pattern: "apiFetch.*timer/history"
---

<objective>
Replace the existing dashboard with a focused Today view cockpit and add session history.

Purpose: The current dashboard shows too much information (weekly goals, rank progress, daily quote) for execution mode. The Today view strips to essentials: what am I working on, how long have I been working, what's next. Session history provides a motivating record of completed work.

Output: A glanceable Today view at "/" with inline timer status, today's goals, streak badge, and a collapsible session history section.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/DISTRIBUTION-MAPPING.md

@apps/desktop/src/pages/DashboardPage.tsx
@apps/desktop/src/stores/dashboard-store.ts
@apps/desktop/src/stores/timer-store.ts
@apps/desktop/src/components/common/Card.tsx
@apps/desktop/src/components/dashboard/PriorityList.tsx
@apps/desktop/src/api/client.ts
@apps/api/src/routes/timer.ts
@apps/api/src/routes/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance timer history API and create history store</name>
  <files>
    apps/api/src/routes/timer.ts
    apps/desktop/src/stores/history-store.ts
  </files>
  <action>
1. In `apps/api/src/routes/timer.ts`, modify the GET /history handler:
   - Add a `select` clause to the findMany that includes `id, focus, goalId, mode, totalWorkedMs, totalBreakMs, pomodoroCount, xpAwarded, startedAt, endedAt`.
   - After fetching sessions, if any have a non-null `goalId`, batch-fetch their goal titles via `db.goal.findMany({ where: { id: { in: goalIds } }, select: { id: true, title: true } })`.
   - Map goal titles onto the session objects as `goalTitle: string | null` before returning.
   - Keep backward compatibility: response shape remains `{ sessions, count }` but each session now includes `goalTitle`.

2. Create `apps/desktop/src/stores/history-store.ts`:
   - Define a `Session` interface matching the enhanced API response: `{ id, focus, goalId, goalTitle, mode, totalWorkedMs, totalBreakMs, pomodoroCount, xpAwarded, startedAt, endedAt }`.
   - Create Zustand store `useHistoryStore` with state: `sessions: Session[]`, `count: number`, `isLoading: boolean`, `error: string | null`.
   - Add `fetchHistory(limit?: number)` action that calls `apiFetch<{ sessions: Session[]; count: number }>('/timer/history?limit=${limit ?? 20}')`.
   - Import `apiFetch` from `../api/client`.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that timer.ts compiles and history-store.ts has correct types</manual>
  </verify>
  <done>GET /timer/history returns sessions with goalTitle field. History store can fetch and hold session data.</done>
</task>

<task type="auto">
  <name>Task 2: Build Today view cockpit with inline timer widget and session history</name>
  <files>
    apps/desktop/src/components/dashboard/TodayView.tsx
    apps/desktop/src/components/dashboard/TimerWidget.tsx
    apps/desktop/src/components/dashboard/SessionHistory.tsx
    apps/desktop/src/pages/DashboardPage.tsx
  </files>
  <action>
1. Create `TimerWidget.tsx`:
   - Read `useTimerStore` for `phase`, `focus`, `getRemainingMs`, `getElapsedMs`, `timerMode`, `pomodoroCount`.
   - If `phase === 'idle'`: Show a subtle "No active session" text with a link/button to navigate to /timer ("Start a session").
   - If timer is active (working/on_break/paused/transition): Show a compact card with:
     - Focus label (the `focus` string) in text-text-primary font-medium.
     - Timer countdown (for pomodoro) or elapsed time (for flowmodoro work) using `useEffect` with a 1-second setInterval to re-render. Format as MM:SS.
     - Phase badge: "Working" (text-brand), "Break" (text-success), "Paused" (text-text-tertiary).
     - If pomodoro, show "Session N" where N is pomodoroCount + 1 (during work) or pomodoroCount (during break).
   - Use the existing Card component wrapper. Keep it compact -- 2-3 lines max.
   - Use `useNavigate` from react-router to make the widget clickable (navigates to /timer).

2. Create `SessionHistory.tsx`:
   - Accept no props; read from `useHistoryStore`.
   - Call `fetchHistory(10)` in a useEffect on mount.
   - Show a Card with title "Recent Sessions".
   - If loading, show LoadingSpinner (small).
   - If no sessions, show "No sessions yet" in text-text-tertiary.
   - For each session, render a row with:
     - Focus label (or "Untitled" if null) in text-text-primary text-sm.
     - Goal title as a small pill/badge below the focus if goalTitle is non-null (text-[10px] uppercase tracking-wider text-text-tertiary bg-surface-2 px-1.5 py-0.5 rounded -- same pattern as timeframe badge in PriorityList).
     - Duration formatted as "Xh Ym" or "Ym" (from totalWorkedMs).
     - Relative time (e.g., "2h ago", "yesterday") for endedAt. Use a simple helper: if < 1h: "Xm ago", < 24h: "Xh ago", else show date as "Mar 22".
   - Max 10 sessions, no pagination needed.

3. Create `TodayView.tsx`:
   - Compose the Today view layout:
     - Top: Greeting (reuse the `getGreeting()` pattern from current DashboardPage) + member displayName.
     - Below greeting: Timer widget (TimerWidget component).
     - Main section: Today's goals using existing PriorityList component with `goals.today` from dashboard store.
     - Sidebar-right (on lg screens): StreakBadge component (existing). Keep only streak -- remove RankProgress, WeeklyGoals, DailyQuote from this view.
     - Below main: SessionHistory component.
   - Layout: Use a grid similar to existing dashboard but simpler. On lg: 2-column (main content + streak sidebar). On mobile: single column stack.
   - Import and use data from `useDashboardStore` for member/goals data.

4. Update `DashboardPage.tsx`:
   - Replace the current dashboard content with `<TodayView />`.
   - Keep the existing `useEffect(() => fetchDashboard())` and loading/error states.
   - Remove imports for WeeklyGoals, RankProgress, DailyQuote (they are no longer rendered on this page).
   - The DashboardPage becomes a thin wrapper that handles loading state and renders TodayView with data.

Design system compliance:
- Dark theme: bg-surface-1, text-text-primary, text-text-secondary, text-text-tertiary.
- Amber accents: text-brand for active elements.
- Card component for sections (rounded-2xl, border-white/5).
- Consistent with existing PriorityList and StreakBadge visual patterns.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open the app, verify the Today view shows greeting, timer widget, today's goals, streak badge, and session history in a clean layout</manual>
  </verify>
  <done>The "/" route shows a focused Today view with inline timer status, today's goals, streak, and recent session history. No weekly goals, rank progress, or daily quote clutter. Glanceable in 2 seconds.</done>
</task>

</tasks>

<verification>
1. `cd apps/api && npx tsc --noEmit` -- API compiles without errors
2. `cd apps/desktop && npx tsc --noEmit` -- Desktop compiles without errors
3. Visual check: "/" route shows Today cockpit view, not the old dashboard
4. Timer widget shows "No active session" when idle, shows countdown when timer is running
5. Session history shows recent sessions with focus labels and durations
</verification>

<success_criteria>
- Member opens the app and immediately sees their current priority and timer status without navigating
- Session history displays past work sessions with focus labels and formatted durations
- The view is stripped to essentials -- no weekly goals grid, no rank progress bar, no daily quote
- Timer widget updates in real-time (1-second interval) when a session is active
</success_criteria>

<output>
After completion, create `.planning/phases/24-desktop-app-enhancement/24-01-SUMMARY.md`
</output>
