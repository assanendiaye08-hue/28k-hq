---
phase: 22-daily-rhythm
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/bot/src/modules/scheduler/planning.ts
  - apps/bot/src/modules/onboarding/setup-flow.ts
  - apps/bot/src/modules/onboarding/commands.ts
  - apps/bot/src/modules/scheduler/index.ts
autonomous: true
requirements: [RHYTHM-03, SET-03]

must_haves:
  truths:
    - "Sunday weekly review DM shows a summary of the week's goal progress before planning"
    - "Weekly review includes goals completed, goals stalled, streak performance, and commitments tracked"
    - "First-time DM interaction triggers a coaching onboarding flow asking timezone and preferences"
    - "Coaching onboarding sets timezone, briefTime, and per-feature toggles on MemberSchedule"
  artifacts:
    - path: "apps/bot/src/modules/scheduler/planning.ts"
      provides: "Weekly review summary showing goal progress, commitment tracking, and activity stats"
      contains: "weekReview"
    - path: "apps/bot/src/modules/onboarding/setup-flow.ts"
      provides: "Coaching onboarding flow for timezone + preferences"
      contains: "coachingOnboarding"
    - path: "apps/bot/src/modules/onboarding/commands.ts"
      provides: "Updated onboarding to include coaching preferences"
  key_links:
    - from: "apps/bot/src/modules/scheduler/planning.ts"
      to: "packages/db/prisma/schema.prisma"
      via: "Query goals completed/stalled this week, commitments fulfilled"
      pattern: "weeklyReview|weekStart"
    - from: "apps/bot/src/modules/onboarding/setup-flow.ts"
      to: "packages/db/prisma/schema.prisma"
      via: "Creates or updates MemberSchedule with timezone and coaching config"
      pattern: "memberSchedule|timezone|coaching"
---

<objective>
Enhance Sunday planning with a weekly review summary and create a coaching onboarding flow for first-time DM interactions.

Purpose: The weekly planning session should open with a data-driven review of the past week (goals completed vs stalled, commitments tracked, activity trends) before asking about next week. The coaching onboarding ensures new members get their timezone and preferences configured on first interaction, so proactive features work correctly from day one without requiring manual /settings configuration.

Output: Enhanced weekly planning with review, coaching onboarding flow integrated into first DM interaction.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-daily-rhythm/22-01-SUMMARY.md

@apps/bot/src/modules/scheduler/planning.ts
@apps/bot/src/modules/onboarding/setup-flow.ts
@apps/bot/src/modules/onboarding/commands.ts
@apps/bot/src/modules/scheduler/index.ts
@apps/bot/src/modules/scheduler/manager.ts
@apps/bot/src/modules/ai-assistant/index.ts
@packages/db/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add weekly review summary to Sunday planning session</name>
  <files>
    apps/bot/src/modules/scheduler/planning.ts
  </files>
  <action>
1. In `apps/bot/src/modules/scheduler/planning.ts`, add a weekly review phase BEFORE the existing "How did last week go?" question:

   a. **Build week review data**: Create a function `buildWeekReview(db, memberId, timezone): Promise<WeekReviewData>` that queries:
      - Goals completed this week (status COMPLETED, completedAt within Mon-Sun range)
      - Goals still active with progress changes (compare currentValue to see if it moved)
      - Goals stalled (active but no currentValue change and no check-in mentioning them)
      - Commitments made this week (from Commitment model, status breakdown: FULFILLED, PENDING, MISSED)
      - Total check-ins this week
      - Total focused time this week (sum voiceSession.durationMinutes + any timer session data)
      - Streak status (current streak, whether it grew or shrank)

   b. **Present review summary**: Before the "How did last week go?" prompt, send an AI-generated or template-based review summary:
      - Use `callAI` with a brief system prompt: "Summarize this member's week in 4-6 sentences. Be factual and specific. Reference actual goal names and numbers. Forward-looking: end with what carries forward to next week."
      - Include the review data as user message context
      - Fallback to template if AI fails:
        ```
        This week: {completedGoals} goals completed, {stalledGoals} stalled.
        {commitmentsFulfilled}/{totalCommitments} commitments kept.
        {checkInCount} check-ins, {focusMinutes} focused minutes.
        Streak: {streakDays} days.
        ```
      - After the summary, THEN ask the existing "How did last week go?" question -- the summary gives the member context to rate their week accurately.

   c. **Store review as conversation message**: Call `storeMessage(db, memberId, 'assistant', reviewText, 'planning')` so the review appears in conversation context. Import storeMessage from `../ai-assistant/memory.js`.

   d. **Goal progress detail**: For each stalled goal, include a brief note in the review: "[Goal title] -- no movement this week." This makes it easy for the member to decide whether to continue, adjust, or drop it during planning.

   Keep all existing planning flow steps (goal setting, reminder parsing, confirmation embed) unchanged. The review is purely additive -- a new opening act before the existing flow.
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord Hustler && npx turbo build --filter=bot 2>&1 | tail -5</automated>
  </verify>
  <done>Sunday planning opens with an AI-generated or template week review showing goals completed/stalled, commitments tracked, check-in count, and focus time. Review stored as conversation message. Existing planning flow preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create coaching onboarding flow for first DM interaction</name>
  <files>
    apps/bot/src/modules/onboarding/setup-flow.ts
    apps/bot/src/modules/onboarding/commands.ts
    apps/bot/src/modules/ai-assistant/index.ts
    apps/bot/src/modules/scheduler/index.ts
  </files>
  <action>
1. In `apps/bot/src/modules/onboarding/setup-flow.ts`, add a new exported function `runCoachingOnboarding(client, db, memberId, discordId): Promise<void>`:

   This is a lightweight DM conversation flow (similar to existing setup-flow pattern):

   a. **Timezone**: "What timezone are you in? (e.g., America/New_York, Europe/London, Asia/Tokyo)"
      - Parse the response: accept common city names/abbreviations and map to IANA timezone strings
      - If invalid, suggest common options and ask again (one retry)
      - Default to UTC if they say "skip" or don't know

   b. **Morning brief time**: "When should I send your morning brief? (e.g., 8am, 9:30am) or 'skip' for no morning brief."
      - Parse time using the same parseReminderTimes pattern from planning.ts
      - Store as briefTime on MemberSchedule

   c. **Coaching level**: "How much should I push you? light (gentle reminders), medium (direct check-ins), heavy (accountability partner)"
      - Map response to accountabilityLevel
      - Default to "medium" if unclear

   d. **Quick confirmation**: Send an embed summarizing: timezone, brief time, coaching level. "Reply 'done' or adjust any setting with /settings anytime."

   e. Create or update MemberSchedule with the collected preferences. Set enableBrief/enableNudge/enableReflection all to true (defaults). Emit 'scheduleUpdated' event so SchedulerManager picks up the new schedule.

2. In `apps/bot/src/modules/ai-assistant/index.ts`, modify the DM handler to trigger coaching onboarding for first-time users:
   - At the top of the DM message handler (before brainstorm check, before pending action check), check if the member has a MemberSchedule record
   - If NO MemberSchedule exists, this is a first-time DM interaction. Run `runCoachingOnboarding(client, db, memberId, discordId)` and return (don't process the message through the normal chat pipeline)
   - After onboarding completes, the member has a schedule and subsequent messages go through normal chat
   - If MemberSchedule exists (even without timezone set), skip onboarding -- they already went through it or used /settings

3. In `apps/bot/src/modules/onboarding/commands.ts`, no changes needed to existing commands. The coaching onboarding is triggered automatically by the DM handler, not by a slash command.

4. In `apps/bot/src/modules/scheduler/index.ts`, ensure the `scheduleUpdated` event handler works for newly created schedules (not just updates). The existing handler already calls `manager.updateMemberSchedule` which handles both cases -- verify this is correct.
  </action>
  <verify>
    <automated>cd /Volumes/Vault/Discord Hustler && npx turbo build --filter=bot 2>&1 | tail -5</automated>
  </verify>
  <done>First DM interaction triggers coaching onboarding: timezone, brief time, coaching level. Creates MemberSchedule with preferences. Subsequent DMs go through normal chat. scheduleUpdated event fires to start per-member cron tasks.</done>
</task>

</tasks>

<verification>
- `npx turbo build --filter=bot` compiles without errors
- planning.ts has buildWeekReview function that queries goals, commitments, check-ins for the week
- planning.ts sends AI-generated or template week review before asking "How did last week go?"
- setup-flow.ts exports runCoachingOnboarding with timezone + brief time + coaching level questions
- ai-assistant/index.ts triggers onboarding for members without MemberSchedule on first DM
- scheduler/index.ts handles scheduleUpdated for new schedules correctly
</verification>

<success_criteria>
1. Sunday planning session opens with a week-in-review showing goals completed/stalled, commitments tracked, and activity stats
2. Week review stored as conversation message for Jarvis context
3. First-time DM user experiences a 3-question coaching onboarding flow
4. Coaching onboarding creates MemberSchedule with timezone, briefTime, accountabilityLevel
5. After onboarding, per-member cron tasks start automatically (brief, nudge, etc.)
6. Bot compiles and all existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/22-daily-rhythm/22-03-SUMMARY.md`
</output>
