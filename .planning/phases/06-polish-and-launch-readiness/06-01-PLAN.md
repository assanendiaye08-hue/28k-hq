---
phase: 06-polish-and-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/shared/constants.ts
  - src/shared/delivery.ts
  - src/modules/server-setup/channels.ts
  - src/modules/onboarding/setup-flow.ts
  - src/modules/resources/constants.ts
  - src/modules/notification-router/router.ts
  - src/modules/notification-router/commands.ts
  - src/modules/notification-router/constants.ts
  - src/modules/notification-router/index.ts
  - src/modules/scheduler/briefs.ts
  - src/modules/ai-assistant/nudge.ts
  - src/modules/xp/index.ts
  - src/modules/voice-tracker/index.ts
  - src/modules/goals/expiry.ts
  - src/modules/checkin/commands.ts
  - src/deploy-commands.ts
autonomous: true
requirements: [FNDN-06]

must_haves:
  truths:
    - "Member can configure which linked account receives each notification type (briefs, nudges, session alerts, level-ups)"
    - "Default behavior (primary account gets everything) works with zero configuration"
    - "Server no longer has hardcoded three-lane Hustle Zones channels"
    - "Notification delivery falls back gracefully when preferred account has DMs closed"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "NotificationPreference and FeedSource/FeedPost/FeedType models"
      contains: "model NotificationPreference"
    - path: "src/modules/notification-router/router.ts"
      provides: "deliverNotification function that routes by notification type"
      exports: ["deliverNotification"]
    - path: "src/modules/notification-router/commands.ts"
      provides: "/notifications command for configuring per-type routing"
      exports: ["buildNotificationsCommand"]
    - path: "src/modules/notification-router/index.ts"
      provides: "Module registration"
      exports: ["name", "register"]
  key_links:
    - from: "src/modules/scheduler/briefs.ts"
      to: "src/modules/notification-router/router.ts"
      via: "deliverNotification with type 'brief'"
      pattern: "deliverNotification.*brief"
    - from: "src/modules/ai-assistant/nudge.ts"
      to: "src/modules/notification-router/router.ts"
      via: "deliverNotification with type 'nudge'"
      pattern: "deliverNotification.*nudge"
    - from: "src/modules/xp/index.ts"
      to: "src/modules/notification-router/router.ts"
      via: "deliverNotification with type 'level_up'"
      pattern: "deliverNotification.*level_up"
---

<objective>
Schema migration for Phase 6 models, per-notification-type account routing (FNDN-06), and server lane cleanup to remove hardcoded Hustle Zones.

Purpose: Establishes the database foundation all Phase 6 features need (NotificationPreference, FeedSource, FeedPost), replaces all deliverToPrivateSpace calls with type-aware routing so multi-account members can direct briefs/nudges/alerts to specific accounts, and removes the rigid three-lane server structure in favor of interest-based channels.

Output: Working /notifications command, all existing notification callers routed through deliverNotification, Hustle Zones replaced with #auto-feed channel, schema migrated with Phase 6 models.
</objective>

<execution_context>
@/Users/ceoassane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ceoassane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-and-launch-readiness/06-RESEARCH.md
@.planning/phases/06-polish-and-launch-readiness/06-CONTEXT.md
@.planning/phases/01-foundation-and-identity/01-03-SUMMARY.md
@.planning/phases/02-daily-engagement-loop/02-03-SUMMARY.md

Key source files:
@prisma/schema.prisma
@src/shared/constants.ts
@src/shared/delivery.ts
@src/modules/server-setup/channels.ts
@src/modules/onboarding/setup-flow.ts
@src/modules/resources/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration, lane cleanup, and notification router core</name>
  <files>
    prisma/schema.prisma
    src/shared/constants.ts
    src/shared/delivery.ts
    src/modules/server-setup/channels.ts
    src/modules/onboarding/setup-flow.ts
    src/modules/resources/constants.ts
    src/modules/notification-router/router.ts
    src/modules/notification-router/constants.ts
  </files>
  <action>
    **Schema migration (prisma/schema.prisma):**
    Add a Phase 6 section with three new models and one new enum:

    1. `FeedSource` model: id (cuid), name (String), type (FeedType enum), url (String), active (Boolean default true), channelId (String?), createdAt (DateTime). Has `posts FeedPost[]` relation.

    2. `FeedPost` model: id (cuid), sourceId (String), externalId (String), title (String), link (String), category (String?), relevanceScore (Int?), upvotes (Int default 0), downvotes (Int default 0), messageId (String?), postedAt (DateTime default now()). Has source FeedSource relation with onDelete Cascade. Unique constraint on [sourceId, externalId]. Index on [postedAt].

    3. `FeedType` enum: RSS, YOUTUBE, REDDIT.

    4. `NotificationPreference` model: id (cuid), memberId (String @unique), briefAccountId (String?), nudgeAccountId (String?), sessionAlertAccountId (String?), levelUpAccountId (String?). Has member Member relation with onDelete Cascade.

    5. Add `notificationPreference NotificationPreference?` relation to the Member model's relations block.

    Run `npx prisma db push` after schema changes.

    **Lane cleanup (3 files):**

    1. `src/shared/constants.ts`: Remove the entire `hustle` property from SERVER_CATEGORIES (lines 62-69 -- the Hustle Zones category with freelancing, ecom, content channels). These channels are no longer created. The resource channels under RESOURCES category remain.

    2. `src/modules/server-setup/channels.ts`: Replace the three resource channels in the RESOURCES category (`tech-resources`, `business-resources`, `growth-resources`) with two channels: keep `tech-resources` and `business-resources` and `growth-resources` as-is (member-shared resources stay), and add a new channel `{ name: 'auto-feed', type: ChannelType.GuildText }` to the RESOURCES category. This keeps human-shared resources separate from AI-curated content per research recommendation.

    3. `src/modules/onboarding/setup-flow.ts`: Update the interests question hint text at line 59 from `"(coding, content creation, ecom, freelancing, design, whatever)"` to `"(coding, design, marketing, writing, business, or whatever you're into)"` -- removes the rigid three-lane examples and uses more open-ended suggestions.

    4. `src/modules/resources/constants.ts`: No changes needed -- the three resource channel names (tech-resources, business-resources, growth-resources) remain valid since we're keeping them.

    **Notification router core (2 new files):**

    1. `src/modules/notification-router/constants.ts`: Export a `NotificationType` type union: `'brief' | 'nudge' | 'session_alert' | 'level_up' | 'general'`. Export a `NOTIFICATION_TYPE_LABELS` object mapping each type to a human-readable label (e.g., `brief: 'Morning Briefs'`, `nudge: 'Accountability Nudges'`, `session_alert: 'Session Invites'`, `level_up: 'Level-Up Alerts'`). Export a `ROUTABLE_TYPES` array of the 4 routable types (excluding 'general').

    2. `src/modules/notification-router/router.ts`: Export `deliverNotification(client, db, memberId, type: NotificationType, content: DeliveryContent): Promise<boolean>`.
       - If type is 'general', delegate directly to existing `deliverToPrivateSpace`.
       - Otherwise, look up `NotificationPreference` for memberId.
       - Extract the account ID for this type (e.g., `prefs.briefAccountId` for 'brief').
       - If a preference exists, try to fetch that user via `client.users.fetch()` and send the content via DM.
       - If that fails (DMs closed, user not found), fall back to `deliverToPrivateSpace` (the existing behavior).
       - If no preference exists, fall back to `deliverToPrivateSpace`.
       - Import `deliverToPrivateSpace` from `../../shared/delivery.js` as the fallback.

    **Update delivery.ts:** Add a comment at the top noting that callers should prefer `deliverNotification` from the notification-router module for typed notifications, and that `deliverToPrivateSpace` remains the low-level fallback.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx prisma db push --accept-data-loss 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Schema has NotificationPreference + FeedSource + FeedPost models. Hustle Zones removed from constants. Router module exports deliverNotification.</manual>
  </verify>
  <done>
    Schema has 5 new Phase 6 artifacts (3 models, 1 enum, 1 relation). Hustle Zones removed from SERVER_CATEGORIES. Setup-flow hints are lane-agnostic. Auto-feed channel added to RESOURCES category. deliverNotification function exists with fallback chain. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire notification routing into all callers and /notifications command</name>
  <files>
    src/modules/notification-router/commands.ts
    src/modules/notification-router/index.ts
    src/modules/scheduler/briefs.ts
    src/modules/ai-assistant/nudge.ts
    src/modules/xp/index.ts
    src/modules/voice-tracker/index.ts
    src/modules/goals/expiry.ts
    src/modules/checkin/commands.ts
    src/deploy-commands.ts
  </files>
  <action>
    **Create /notifications command (2 new files):**

    1. `src/modules/notification-router/commands.ts`:
       - Export `buildNotificationsCommand()` returning a SlashCommandBuilder with name 'notifications' and description 'Choose which account gets each notification type'.
       - Add a subcommand `view` that shows current routing preferences as an embed. For each routable type, show the assigned account (mention format) or "Default (primary account)".
       - Add a subcommand `set` with a string option `type` (required, choices from ROUTABLE_TYPES) and a user option `account` (required -- the Discord account to route to). Handler: look up the member from the interaction user's discordId, verify the target account is actually linked to the same member (query DiscordAccount where discordId = account.id AND memberId matches). If not linked, reply ephemeral "That account isn't linked to your profile." If linked, upsert NotificationPreference setting the appropriate field. Reply ephemeral confirmation.
       - Add a subcommand `reset` with a string option `type` (required, choices from ROUTABLE_TYPES). Handler: set the corresponding field to null in NotificationPreference. Reply ephemeral confirmation.
       - Export the command handler function.

    2. `src/modules/notification-router/index.ts`:
       - Standard module registration following existing pattern: export name = 'notification-router', register function that calls ctx.commands.register for the /notifications command.
       - Module will be auto-discovered by the module loader.

    **Wire deliverNotification into all existing callers (6 files):**

    Replace `deliverToPrivateSpace` with `deliverNotification` in these files, changing the import from `../../shared/delivery.js` to `../notification-router/router.js` (or appropriate relative path) and adding the notification type parameter:

    1. `src/modules/scheduler/briefs.ts`: Two calls -- both use type `'brief'`. Change import to deliverNotification. Replace both `deliverToPrivateSpace(client, db, memberId, { embeds: [...] })` with `deliverNotification(client, db, memberId, 'brief', { embeds: [...] })`. The check-in reminder delivery on line ~479 also uses type `'brief'` (it's part of the morning engagement system).

    2. `src/modules/ai-assistant/nudge.ts`: One call -- use type `'nudge'`. Replace the call around line 256.

    3. `src/modules/xp/index.ts`: One call -- use type `'level_up'`. This is the level-up celebration embed delivery around line 72.

    4. `src/modules/voice-tracker/index.ts`: Two calls -- use type `'general'` (voice session summaries aren't a distinct notification category worth routing). Lines ~94 and ~135.

    5. `src/modules/goals/expiry.ts`: One call -- use type `'general'` (goal expiry warnings). Line ~51.

    6. `src/modules/checkin/commands.ts`: One call -- use type `'general'` (check-in confirmation). Line ~253.

    Note: `src/modules/data-privacy/commands.ts` uses deliverToPrivateSpace for /mydata file delivery -- leave this unchanged since it's a one-time export action, not a recurring notification.

    **Update deploy-commands.ts:**
    Import `buildNotificationsCommand` from the notification-router module. Add it to the commands array. Update the Phase comment header to include Phase 6. Total commands becomes 23.
  </action>
  <verify>
    <automated>cd "/Volumes/Vault/Discord Hustler" && npx tsc --noEmit 2>&1 | head -20 && grep -r "deliverToPrivateSpace" src/modules/ --include="*.ts" | grep -v "data-privacy" | grep -v "notification-router" | wc -l</automated>
    <manual>The grep count should be 0 -- all callers except data-privacy now use deliverNotification. /notifications command builds without errors.</manual>
  </verify>
  <done>
    All recurring notification callers (briefs, nudges, level-ups, voice summaries, goal expiry, check-in confirmations) route through deliverNotification. /notifications command registered with view/set/reset subcommands. Deploy script registers 23 total commands. Only data-privacy/commands.ts still uses deliverToPrivateSpace directly (correct -- it's a file export, not a recurring notification). TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles with zero errors
2. `npx prisma db push` succeeds with new models
3. `grep -r "Hustle Zones" src/` returns no results
4. `grep -r "deliverToPrivateSpace" src/modules/ --include="*.ts" | grep -v data-privacy | grep -v notification-router` returns 0 lines
5. `grep -c "deliverNotification" src/modules/scheduler/briefs.ts` returns 2 (two call sites)
6. `grep -c "deliverNotification" src/modules/ai-assistant/nudge.ts` returns 1
</verification>

<success_criteria>
- NotificationPreference model exists in Prisma schema and DB is pushed
- FeedSource, FeedPost, FeedType exist in Prisma schema (ready for Plan 02)
- deliverNotification routes to preferred account when configured, falls back to deliverToPrivateSpace when not
- /notifications view/set/reset subcommands work for 4 notification types
- Hustle Zones category removed from constants.ts
- Auto-feed channel added to RESOURCES category in channels.ts
- Setup flow hints are lane-agnostic
- All 7 existing deliverToPrivateSpace callers (except data-privacy) migrated to deliverNotification
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-launch-readiness/06-01-SUMMARY.md`
</output>
