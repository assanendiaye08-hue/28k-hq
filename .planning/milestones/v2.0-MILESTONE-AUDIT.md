---
milestone: v2.0
audited: 2026-03-22
status: tech_debt
scores:
  requirements: 51/55
  phases: 6/6
  integration: 21/22
  flows: 5/6
gaps:
  requirements:
    - id: "AUTH-03"
      status: "partial"
      phase: "15"
      evidence: "logoutSession() sends no Authorization header; server rejects with 401; refresh token persists in DB"
    - id: "AUTH-04"
      status: "partial"
      phase: "15"
      evidence: "Tokens stored in plugin-store (unencrypted JSON), not OS keychain as specified"
    - id: "APP-06"
      status: "partial"
      phase: "19"
      evidence: "Updater pubkey and endpoint URL are placeholder strings; functional toggle but cannot download updates"
  integration:
    - from: "desktop logoutSession()"
      to: "API POST /auth/logout"
      issue: "Missing Authorization header causes 401; server token never deleted"
tech_debt:
  - phase: 15-rest-api-authentication
    items:
      - "Logout fetch missing auth header — refresh token persists in DB after logout"
      - "Tokens in plugin-store (JSON file) not OS keychain"
  - phase: 17-pomodoro-timer
    items:
      - "Tray icon is placeholder amber circle, not gold ouroboros"
  - phase: 19-cross-platform-sync-polish
    items:
      - "Auto-updater pubkey and endpoint URL are placeholders"
      - "SYNC-02 (bot DM on timer complete) intentionally skipped"
      - "APP-03/TMR-07 (popover) intentionally changed to main window"
---

# v2.0 Milestone Audit: Desktop Companion App

## Summary

| Metric | Score |
|--------|-------|
| Requirements | 51/55 satisfied (4 partial) |
| Phases | 6/6 complete |
| Integration | 21/22 connections wired |
| E2E Flows | 5/6 complete (logout partially broken) |

## Requirements Coverage

### Satisfied (51)

All INFRA, API, TMR, FLW, GOAL, DASH, and SYNC requirements are fully wired and functional.

### Partial (4)

| ID | Issue | Severity |
|----|-------|----------|
| AUTH-03 | Logout doesn't invalidate server-side refresh token | Medium |
| AUTH-04 | Tokens in JSON file, not OS keychain | Low |
| APP-02 | Tray icon is placeholder, not ouroboros | Cosmetic |
| APP-06 | Auto-updater needs real signing key + endpoint | Pre-release |

### Intentionally Changed (3)

| ID | Original | Changed To |
|----|----------|-----------|
| SYNC-02 | Bot DM on timer complete | App shows transition screen instead |
| APP-03 | Popover mini-window | Single main window |
| TMR-07 | Popover on tray click | Main window navigation |

## E2E Flow Results

| Flow | Status |
|------|--------|
| Login (OAuth → token → dashboard) | ✓ Complete |
| Timer (start → countdown → stop → XP → refresh) | ✓ Complete |
| Goals (create → progress → complete → XP → refresh) | ✓ Complete |
| Settings (toggles → persist → restart) | ✓ Complete |
| Navigation (sidebar links) | ✓ Complete |
| Logout (local clear → server invalidate) | ⚠ Partial — server token persists |

## Tech Debt

1. **Logout auth header** — logoutSession() needs Authorization header or API needs to remove authenticate guard on /auth/logout
2. **Token storage** — plugin-store is unencrypted JSON; production should use OS keychain
3. **Tray icon** — placeholder amber circle; needs final branded ouroboros asset
4. **Auto-updater** — signing key and GitHub Releases endpoint need configuration before distribution
