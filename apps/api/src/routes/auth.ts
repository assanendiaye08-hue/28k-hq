import type { FastifyInstance } from 'fastify';

// Placeholder -- auth routes implemented in Task 2
export default async function authRoutes(_fastify: FastifyInstance) {
  // Routes will be added in Task 2
}
